---
phase: 05-rename
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - package.json
  - src/index.ts
autonomous: true
requirements:
  - RENAME-01
  - RENAME-02
  - RENAME-03
  - RENAME-04
user_setup: []
tags:
  - typescript
  - rename
  - mcp-server
  - env-vars
  - deprecation

must_haves:
  truths:
    - "package.json `name` is `@momidala/legate`"
    - "package.json `bin` map exposes `legate` -> `./build/cli.js` and `legate-mcp` -> `./build/index.js`"
    - "src/index.ts registers MCP server with name `legate` (not `prefect`)"
    - "All 40 tool registrations in src/index.ts use the `legate_` prefix (zero `prefect_` literal strings remain in src/index.ts)"
    - "src/index.ts BASE_URL chain reads LEGATE_SERVER_URL first, falls back to PREFECT_SERVER_URL with one-time deprecation warning, then OPENCODE_URL with its existing one-time warning (brand updated to [Legate])"
    - "src/index.ts TIMEOUT_MS chain reads LEGATE_TIMEOUT_MS first, falls back to PREFECT_TIMEOUT_MS with one-time deprecation warning"
    - "All `[Prefect]` console.error prefixes in src/index.ts are now `[Legate]`"
    - "All cross-tool references in tool .describe() text use `legate_*` (e.g. `Consider using legate_session_rename`)"
    - "`npm run build` succeeds — TypeScript compiles with no errors after package.json + index.ts changes"
  artifacts:
    - path: "package.json"
      provides: "Package identity and binaries"
      contains: "@momidala/legate"
    - path: "src/index.ts"
      provides: "MCP server with legate_* tool names, LEGATE_* env vars, three-tier BASE_URL chain, two-tier TIMEOUT_MS chain"
      contains: "new McpServer({ name: 'legate'"
  key_links:
    - from: "package.json bin"
      to: "src/index.ts (compiled to build/index.js) and src/cli.ts (compiled to build/cli.js)"
      via: "npm install -g exposes binaries via package.json bin map"
      pattern: "\"legate\": \"./build/cli.js\""
    - from: "src/index.ts McpServer registration"
      to: "registerTool('legate_*'...) for all 40 tools"
      via: "MCP stdio tool exposure"
      pattern: "registerTool\\('legate_"
    - from: "src/index.ts BASE_URL chain"
      to: "process.env LEGATE_SERVER_URL/PREFECT_SERVER_URL/OPENCODE_URL"
      via: "Three-tier env var fallback with one-time warnings"
      pattern: "LEGATE_SERVER_URL.*PREFECT_SERVER_URL.*OPENCODE_URL"
---

<objective>
Rename the package identity (`@momidala/prefect` → `@momidala/legate`), CLI binary names in package.json (`prefect`/`prefect-mcp` → `legate`/`legate-mcp`), and the entire src/index.ts file: MCP server registration name, all 40 `prefect_*` tool names → `legate_*`, the BASE_URL three-tier env var chain (LEGATE_ → PREFECT_ → OPENCODE_), the TIMEOUT_MS two-tier chain (LEGATE_ → PREFECT_), all `[Prefect]` log prefixes → `[Legate]`, and all cross-tool references inside tool descriptions.

Purpose: Implement RENAME-01, RENAME-02, RENAME-03, and the portion of RENAME-04 that lives in src/index.ts (BASE_URL and TIMEOUT_MS env var chains). This is the package identity + MCP server entry-point pass. Plan 02b will then rename the remaining env-var-reading source modules and update test files.

Output: A buildable package.json + src/index.ts where `npm run build` succeeds. Tests will be partially broken until Plan 02b runs (Plan 01's RED deprecation tests for auth/autostart/sessions still fail; pre-existing tests referencing PREFECT_* env vars also still fail). The build artifact is `build/index.js` exposed as the `legate-mcp` binary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-rename/05-RESEARCH.md
@.planning/phases/05-rename/05-PATTERNS.md
@.planning/phases/05-rename/05-VALIDATION.md
@.planning/phases/05-rename/05-01-SUMMARY.md

<interfaces>
<!-- Concrete target signatures and identifiers — executor uses these directly -->

package.json target:
- "name": "@momidala/legate"   (was @momidala/prefect)
- "bin": { "legate": "./build/cli.js", "legate-mcp": "./build/index.js" }   (was prefect/prefect-mcp)
- Every other top-level field (version, description, repository, scripts, dependencies, devDependencies) UNCHANGED

src/index.ts targets:
- new McpServer({ name: 'legate', version: packageVersion })   (was 'prefect')
- 40 server.registerTool('legate_*', ...) calls — exact list per 05-RESEARCH.md "Complete Tool Name Rename Map"
- BASE_URL three-tier chain: LEGATE_SERVER_URL → PREFECT_SERVER_URL (warn once) → OPENCODE_URL (warn once, preserved)
- TIMEOUT_MS two-tier chain: LEGATE_TIMEOUT_MS → PREFECT_TIMEOUT_MS (warn once); no OPENCODE_ tier
- All console.error '[Prefect]' prefixes → '[Legate]'
- Tool description cross-references like 'Consider using prefect_session_rename' → 'Consider using legate_session_rename'
</interfaces>

<verification_greps>
After source changes, these greps must produce the indicated counts:
- `grep -v '^#' src/index.ts | grep -c "prefect"` → 0
- `grep -v '^#' src/index.ts | grep -c "PREFECT"` → 0
- `grep -c "\\[Prefect\\]" src/index.ts` → 0
- `grep -c "registerTool('legate_" src/index.ts` → 40
- `grep -c "registerTool('prefect_" src/index.ts` → 0
- `grep -c "@momidala/prefect" package.json` → 0
- `grep -c "@momidala/legate" package.json` → 1
</verification_greps>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename package.json + src/index.ts (package identity, MCP server name, 40 tool names, BASE_URL and TIMEOUT_MS chains)</name>
  <files>package.json, src/index.ts</files>
  <read_first>
    - package.json (current name, bin, version — must preserve every field except name/bin keys)
    - src/index.ts (entire file — McpServer registration line 94, BASE_URL block lines 15-23, TIMEOUT_MS line 24, all 40 registerTool calls, all .describe() strings with PREFECT_*/prefect_* references, all '[Prefect]' console.error prefixes)
    - .planning/phases/05-rename/05-PATTERNS.md (section "src/index.ts" — three change types and exact target patterns)
    - .planning/phases/05-rename/05-RESEARCH.md ("Complete Tool Name Rename Map" with all 40 names; "Complete Env Var Rename Map" with chain structure)
  </read_first>
  <action>
    Modify package.json: set "name" to "@momidala/legate"; rename bin keys: "prefect" → "legate", "prefect-mcp" → "legate-mcp" (preserve the path values "./build/cli.js" and "./build/index.js"). Do NOT touch version, description, repository, scripts, dependencies, devDependencies, or any other field.

    Modify src/index.ts:
    1. Line 94: change McpServer({ name: 'prefect', ... }) to McpServer({ name: 'legate', ... }).
    2. Replace the BASE_URL block (currently a two-tier PREFECT_SERVER_URL → OPENCODE_URL fallback) with a three-tier chain that prefers LEGATE_SERVER_URL, falls back to PREFECT_SERVER_URL with one-time '[Legate] PREFECT_SERVER_URL is deprecated, use LEGATE_SERVER_URL' warning, then falls back to OPENCODE_URL with its existing one-time warning (updated brand to '[Legate]'), then defaults to 'http://localhost:4096'. Add a module-level boolean flag warnedServerUrl initialized to false to guard the PREFECT_SERVER_URL warning. The OPENCODE_URL warning may remain ungated or get its own flag — match the structure used in src/auth.ts.
    3. Replace the TIMEOUT_MS read with a two-tier chain via a helper function (e.g. resolveTimeoutMs): prefer LEGATE_TIMEOUT_MS, fall back to PREFECT_TIMEOUT_MS with one-time '[Legate] PREFECT_TIMEOUT_MS is deprecated, use LEGATE_TIMEOUT_MS' warning. Add module-level warnedTimeoutMs flag. Default value 120_000 preserved.
    4. Rename every registerTool('prefect_*', ...) to registerTool('legate_*', ...) — all 40 listed in 05-RESEARCH.md "Complete Tool Name Rename Map". Use a complete grep-then-edit pass; do not rely on memory of the count.
    5. Update every cross-tool reference inside .describe() text (e.g. 'Consider using prefect_session_rename') to use 'legate_*' instead.
    6. Update every env var name appearing inside tool .describe() text from PREFECT_* to LEGATE_*.
    7. Replace every '[Prefect]' literal in console.error calls with '[Legate]'.
    8. Final verification: grep -c "prefect" src/index.ts must return 0, grep -c "PREFECT" src/index.ts must return 0, grep -c "registerTool\\('legate_" src/index.ts must return 40.

    Do NOT change SESSIONS_DIR or REGISTRY_DIR strings in index.ts if any are read (they reside in sessions.ts/registry.ts and stay 'prefect' — out of scope).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/larry/Documents/repos/momidala/prefect && npm run build 2>&1 | tail -10 && grep -v '^#' src/index.ts | grep -c "prefect\|PREFECT" && grep -c "registerTool('legate_" src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "@momidala/legate" package.json equals 1
    - grep -c "@momidala/prefect" package.json equals 0
    - grep -c '"legate":' package.json >= 1 (bin key)
    - grep -c '"legate-mcp":' package.json equals 1
    - grep -c '"prefect":' package.json equals 0
    - grep -c '"prefect-mcp":' package.json equals 0
    - grep -v '^#' src/index.ts | grep -c "prefect" equals 0
    - grep -v '^#' src/index.ts | grep -c "PREFECT" equals 0
    - grep -c "registerTool('legate_" src/index.ts equals 40
    - grep -c "registerTool('prefect_" src/index.ts equals 0
    - grep -c "name: 'legate'" src/index.ts >= 1
    - grep -c "LEGATE_SERVER_URL" src/index.ts >= 1
    - grep -c "LEGATE_TIMEOUT_MS" src/index.ts >= 1
    - grep -c "\\[Legate\\]" src/index.ts >= 2 (deprecation warnings)
    - grep -c "\\[Prefect\\]" src/index.ts equals 0
    - `npm run build` exits 0 (TypeScript compiles)
  </acceptance_criteria>
  <done>package.json identity renamed; src/index.ts has 40 legate_* tool registrations, three-tier BASE_URL chain, two-tier TIMEOUT_MS chain, McpServer name 'legate', and zero `prefect`/`PREFECT` string occurrences; `npm run build` succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env → MCP server | Untrusted env vars (LEGATE_*, PREFECT_*, OPENCODE_*) read at module load via BASE_URL and TIMEOUT_MS chains |
| stderr → MCP host (Claude Code) | Deprecation warnings cross to Claude Code's MCP log stream |
| stdio MCP protocol | Tool name strings sent to MCP host MUST be the registered legate_* names; mismatch breaks the protocol |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | console.error deprecation warnings in src/index.ts could leak secret values | mitigate | BASE_URL and TIMEOUT_MS warning code paths log env var NAMES only (per 05-PATTERNS.md target patterns). Acceptance criteria check warning brand strings; no acceptance criterion permits value logging. |
| T-05-06 | Tampering | env var fallback chain ordering could be reversed by accident, causing PREFECT_SERVER_URL to win over LEGATE_SERVER_URL | mitigate | The ?? null-coalescing precedence is unidirectional; reversing it would also reverse precedence visibly. Plan 02b's test updates will catch regression at npm test time. |
| T-05-08 | Tampering | Tool name string mismatch (registering as `legate_*` but documenting/exposing as `prefect_*`) breaks MCP protocol | mitigate | Acceptance criterion `grep -c "registerTool('legate_" src/index.ts equals 40` and `grep -c "registerTool('prefect_" src/index.ts equals 0` catches all 40 names. |
| T-05-09 | Denial of Service | Replacing the OPENCODE_URL fallback rather than extending it could break users still on OPENCODE_URL | mitigate | Action step 2 explicitly preserves the OPENCODE_URL tier (with brand-updated warning); reviewer can grep for `OPENCODE_URL` in src/index.ts. |
| T-05-11 | Repudiation | one-time-warn guard may mask repeated misuse of deprecated PREFECT_SERVER_URL | accept | Intentional UX (RENAME-04 requirement). Solo dev project. |
</threat_model>

<verification>
- `npm run build` exits 0 with no TypeScript errors
- `grep -v '^#' src/index.ts | grep -c "prefect\\|PREFECT"` returns 0
- `grep -c "registerTool('legate_" src/index.ts` returns exactly 40
- `grep -c "@momidala/legate" package.json` returns 1
- `grep -c "@momidala/prefect" package.json` returns 0
- `grep -c "\\[Prefect\\]" src/index.ts` returns 0
- Build artifact `build/index.js` exists after build (chmod 755 per package.json build script)
- Tests are NOT expected to pass at this point — Plan 02b finishes the source rename and updates tests
</verification>

<success_criteria>
- Acceptance criteria for RENAME-01 (package name), RENAME-02 (binary names), RENAME-03 (40 tool names), and the src/index.ts portion of RENAME-04 (BASE_URL + TIMEOUT_MS chains) all satisfied
- `npm run build` succeeds
- Three-tier BASE_URL chain (LEGATE_ → PREFECT_ → OPENCODE_) preserved
- Two-tier TIMEOUT_MS chain (LEGATE_ → PREFECT_) implemented
- All `[Prefect]` literals in src/index.ts converted to `[Legate]`
</success_criteria>

<output>
After completion, create `.planning/phases/05-rename/05-02-SUMMARY.md` documenting:
- Final `grep -c "prefect"` and `grep -c "PREFECT"` counts in package.json and src/index.ts
- `npm run build` output snippet showing success
- Note for Plan 02b: src/index.ts is now fully renamed but tests remain broken until Plan 02b finishes the other 7 source files and updates the 6 test files
</output>
