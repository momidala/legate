---
phase: 05-rename
plan: 03
type: execute
wave: 4
depends_on: ["05-02b"]
files_modified:
  - README.md
  - EXAMPLE_CLAUDE.md
  - AGENTS.md
  - CLAUDE.md
  - examples/test-task.md
  - examples/uat-v2.md
  - .gitattributes
autonomous: false
requirements:
  - DOC-01
  - DOC-02
user_setup: []
tags:
  - documentation
  - migration
  - gitattributes

must_haves:
  truths:
    - "README.md references @momidala/legate, legate / legate-mcp binaries, legate_* tools, LEGATE_* env vars, and includes a Migration-from-@momidala/prefect section"
    - "EXAMPLE_CLAUDE.md, AGENTS.md, examples/test-task.md, examples/uat-v2.md, project CLAUDE.md all reference legate / legate_* / LEGATE_* with no stray prefect references outside the README migration note"
    - "A new file .gitattributes exists at the project root containing `*.ts linguist-language=TypeScript`"
    - "Final human verification confirms README install instructions are accurate and the migration note is helpful"
    - "Config-directory path ~/.config/prefect/ is preserved in docs where it correctly refers to runtime state location"
  artifacts:
    - path: "README.md"
      provides: "Primary user-facing documentation with new branding and migration note"
      contains: "@momidala/legate"
    - path: "EXAMPLE_CLAUDE.md"
      provides: "Example project CLAUDE.md showing legate setup"
      contains: "legate_create_session"
    - path: "AGENTS.md"
      provides: "Agent-facing reference"
      contains: "LEGATE_"
    - path: "CLAUDE.md"
      provides: "Project-root Claude instructions"
      contains: "legate_"
    - path: "examples/test-task.md"
      provides: "End-to-end smoke test instructions"
      contains: "legate_"
    - path: "examples/uat-v2.md"
      provides: "Full UAT script"
      contains: "@momidala/legate"
    - path: ".gitattributes"
      provides: "GitHub Linguist override so .ts files are classified as TypeScript"
      contains: "*.ts linguist-language=TypeScript"
  key_links:
    - from: "README.md"
      to: "package.json"
      via: "install command `npm install -g @momidala/legate` matches package.json name"
      pattern: "@momidala/legate"
    - from: "README.md"
      to: "src/cli.ts handleInstallCommand"
      via: "documents `legate install-command` produces ~/.claude/commands/legate-update.md"
      pattern: "legate install-command"
    - from: ".gitattributes"
      to: "GitHub repo language stats"
      via: "GitHub Linguist reads file at repo root"
      pattern: "linguist-language=TypeScript"
---

<objective>
Update all user-facing documentation to reflect the Legate rename (DOC-01) and add a `.gitattributes` file so GitHub Linguist classifies `.ts` files as TypeScript instead of JavaScript (DOC-02). Include a migration section in README.md that explicitly references the old `@momidala/prefect` package and `PREFECT_*` env vars so existing users know how to move over.

Purpose: Complete the user-visible surface of the rename. Plan 02 shipped the code; this plan ships the words that explain it. The migration note is the documented migration path required by RENAME-04's soft-migration story.

Output: Six existing documentation files updated and one new `.gitattributes` file. Human verification of the README ends the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-rename/05-RESEARCH.md
@.planning/phases/05-rename/05-PATTERNS.md
@.planning/phases/05-rename/05-VALIDATION.md
@.planning/phases/05-rename/05-02-SUMMARY.md
@.planning/phases/05-rename/05-02b-SUMMARY.md

<interfaces>
<!-- Source-of-truth strings for documentation -->

Package name: @momidala/legate   (was @momidala/prefect)
Install command: npm install -g @momidala/legate
Binary names: legate, legate-mcp   (was prefect, prefect-mcp)
MCP server key in .mcp.json: legate   (was prefect)
Slash command file installed by CLI: ~/.claude/commands/legate-update.md   (was prefect-update.md)
Tool name prefix in Claude Code: legate_*   (was prefect_*) — 40 tools total per 05-RESEARCH.md "Complete Tool Name Rename Map"
Env var prefix: LEGATE_*   (was PREFECT_*) — 7 vars total
Brand prefix in logs: [Legate]   (was [Prefect])
Console branding (capitalized prose): Legate   (was Prefect)

Seven search-and-replace rules from 05-PATTERNS.md "Documentation Pattern Assignments":
- prefect_   ->   legate_      (tool name prefix, lowercase + underscore)
- PREFECT_   ->   LEGATE_      (env var prefix, all-caps + underscore)
- Prefect    ->   Legate       (capitalized brand in prose)
- prefect    ->   legate       (binary/command name, lowercase standalone-word match)
- prefect-mcp ->  legate-mcp   (binary name with hyphen)
- @momidala/prefect -> @momidala/legate
- prefect-update -> legate-update  (slash command name)
- ~/.config/prefect/  ->  UNCHANGED  (config dir is out of scope; preserve every occurrence that refers to the runtime config directory location)

Migration note (REQUIRED in README.md only; source: 05-RESEARCH.md "Migration Note Template"):
- Title: "Migrating from @momidala/prefect" (heading MUST contain the literal string '@momidala/prefect' so users searching for it land here)
- Steps: (1) uninstall old global package, (2) install new global package, (3) re-run `legate init --force` to update .mcp.json key, (4) rename env vars in shell profile or .mcp.json env block (note that PREFECT_* still works with deprecation warning during transition), (5) note that existing sessions in ~/.config/prefect/ are preserved
</interfaces>

<verification_greps>
Post-edit greps must produce these results:
- grep -c "@momidala/legate" README.md returns >= 2 (install command + migration note new package)
- @momidala/prefect appears in README.md ONLY inside the migration note (<= 3 occurrences acceptable; all must be within the migration section)
- prefect_ (tool prefix) appears in README.md ONLY inside migration code-fence blocks demonstrating old vs new (otherwise 0)
- PREFECT_ appears in README.md ONLY inside migration note examples (<= 7 occurrences acceptable — one per env var as a deprecated reference)
- grep -rn "prefect_|PREFECT_|@momidala/prefect|prefect-mcp" against EXAMPLE_CLAUDE.md AGENTS.md examples/test-task.md examples/uat-v2.md returns 0 results
- In project CLAUDE.md (repo root), grep -c "prefect_" CLAUDE.md is 0 and grep -c "PREFECT_" CLAUDE.md is 0
- cat .gitattributes outputs "*.ts linguist-language=TypeScript"
- grep -rn "\.config/prefect" README.md EXAMPLE_CLAUDE.md AGENTS.md examples/ MAY return results — these are correct (config dir is out of scope and docs must keep referring to ~/.config/prefect/)
</verification_greps>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md and add Migration section</name>
  <files>README.md</files>
  <read_first>
    - README.md (entire file — 108 prefect-containing lines per 05-RESEARCH.md File Surface Map; current install/usage instructions; current tool name examples)
    - .planning/phases/05-rename/05-PATTERNS.md (Documentation Pattern Assignments table — seven replacement rules)
    - .planning/phases/05-rename/05-RESEARCH.md ("Migration Note Template" exact wording recommendation)
    - package.json (target name string for install instructions)
  </read_first>
  <action>
    Read README.md in full first. Apply the documentation rename rules in order of specificity to avoid double-substitution:

    1. Replace every occurrence of '@momidala/prefect' with '@momidala/legate' EXCEPT inside the migration section you will add in step 8.
    2. Replace every 'prefect-update' (slash command name) with 'legate-update'.
    3. Replace every 'prefect-mcp' (binary name) with 'legate-mcp'.
    4. Replace every 'prefect_' tool name prefix (e.g. prefect_create_session, prefect_run, prefect_get_diff — full list in 05-RESEARCH.md "Complete Tool Name Rename Map") with 'legate_'.
    5. Replace every 'PREFECT_' env var prefix with 'LEGATE_' (covers all 7 env vars).
    6. Replace standalone occurrences of 'prefect' (lowercase, word-boundary) with 'legate' when they refer to the CLI binary or npm package short name. SKIP occurrences inside paths like '~/.config/prefect/' (those are runtime state, out of scope per REQUIREMENTS.md).
    7. Replace 'Prefect' (capitalized prose brand) with 'Legate' wherever the project itself is being named.
    8. Add a new top-level section near the end of the README titled '## Migrating from @momidala/prefect' (heading MUST contain the literal string '@momidala/prefect' so users searching for it land here). Section contents adapted from 05-RESEARCH.md "Migration Note Template": step 1 uninstall old global package using npm uninstall -g @momidala/prefect, step 2 install new global package using npm install -g @momidala/legate, step 3 re-run legate init with --force flag to overwrite the existing prefect key in .mcp.json with legate, step 4 rename PREFECT_* env vars to LEGATE_* in shell profile (.bashrc/.zshrc) or in .mcp.json env blocks (noting PREFECT_* continues to work with a one-time deprecation warning per process), step 5 note that existing sessions in ~/.config/prefect/ are preserved (no manual cleanup required). This migration section is the ONE place where 'prefect' / 'PREFECT_' / '@momidala/prefect' appear by design — those references are intentional.
    9. Verify all references to install location and config directory '~/.config/prefect/' are preserved literally (out of scope per REQUIREMENTS.md).

    After edits, run the verification greps from the verification_greps block above to confirm counts.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/larry/Documents/repos/momidala/prefect && grep -c "@momidala/legate" README.md && grep -c "Migrating from" README.md && grep -c "legate_" README.md && grep -c "LEGATE_" README.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "@momidala/legate" README.md is >= 2
    - grep -c "Migrating from @momidala/prefect" README.md equals 1 (heading present)
    - grep -c "legate_create_session" README.md is >= 1 (at least one tool reference)
    - grep -c "legate_run" README.md is >= 1
    - grep -c "LEGATE_SERVER_URL" README.md is >= 1
    - grep -c "legate-mcp" README.md is >= 1
    - grep -c "npm install -g @momidala/legate" README.md is >= 1
    - All occurrences of '@momidala/prefect' in README.md are inside lines also within +/- 30 lines of the 'Migrating from' heading (acceptable migration-context references)
    - All occurrences of 'PREFECT_' in README.md are within the migration section
    - grep -n "\.config/prefect" README.md returns at least one line referring to the runtime config dir (preserved)
    - No occurrences of 'prefect_' (tool prefix) outside the migration section
    - No occurrences of 'prefect-mcp' outside the migration section
    - No occurrences of 'prefect-update' (renamed to legate-update)
  </acceptance_criteria>
  <done>README.md renamed throughout with a clearly-marked Migration section; all seven doc replacement rules applied; runtime config dir path references preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Rename EXAMPLE_CLAUDE.md, AGENTS.md, project CLAUDE.md, examples/test-task.md, examples/uat-v2.md and create .gitattributes</name>
  <files>EXAMPLE_CLAUDE.md, AGENTS.md, CLAUDE.md, examples/test-task.md, examples/uat-v2.md, .gitattributes</files>
  <read_first>
    - EXAMPLE_CLAUDE.md (entire file — 26 prefect refs per 05-RESEARCH.md)
    - AGENTS.md (entire file — 6 prefect refs)
    - CLAUDE.md (the project-root file at /mnt/c/Users/larry/Documents/repos/momidala/prefect/CLAUDE.md — NOT the global one in ~/.claude/. ~20 prefect refs)
    - examples/test-task.md (entire file — 17 prefect refs)
    - examples/uat-v2.md (entire file — 163 prefect refs; comprehensive UAT script)
    - .planning/phases/05-rename/05-PATTERNS.md ("Documentation Pattern Assignments" — same seven rules as Task 1 but no migration-note exception for these files)
    - .planning/phases/05-rename/05-RESEARCH.md (".gitattributes" syntax example and Linguist note)
  </read_first>
  <action>
    For each of the five documentation files (EXAMPLE_CLAUDE.md, AGENTS.md, CLAUDE.md, examples/test-task.md, examples/uat-v2.md), apply the documentation rename rules from Task 1 (steps 1-7) in the same specificity order to avoid double-substitution.

    Unlike README.md, these files do NOT need a migration note section. They are forward-looking docs; the README is the canonical migration entry point. After the rename pass, ZERO prefect-prefix tokens (prefect_, PREFECT_, prefect-mcp, prefect-update, @momidala/prefect) should remain in these five files. The only allowed prefect substring is inside the literal path '~/.config/prefect/' which must be preserved.

    For examples/uat-v2.md specifically (163 occurrences): this is a comprehensive UAT script. After substitutions, double-check inline code snippets (markdown fenced blocks with bash / typescript / json) — these often contain CLI commands like 'prefect_run' that need updating. The path strings '~/.config/prefect/sessions.json' and '~/.config/prefect/servers.json' must remain literal.

    For project CLAUDE.md (/mnt/c/Users/larry/Documents/repos/momidala/prefect/CLAUDE.md): apply same rules. The heading "When to Use the Prefect Tools" becomes "When to Use the Legate Tools". The Tool Reference table updates prefect_* -> legate_*. Env var references update PREFECT_* -> LEGATE_*. Binary references update prefect-mcp -> legate-mcp. Server URL examples update PREFECT_SERVER_URL -> LEGATE_SERVER_URL. The phrase "Prefect is therefore" or similar prose becomes "Legate is therefore". The reference to "the prefect tools" becomes "the legate tools".

    Create a NEW file at the project root path .gitattributes containing exactly this single line followed by a trailing newline: an asterisk followed by ".ts" followed by a space followed by "linguist-language=TypeScript". No comments, no blank lines, no BOM marker. This forces GitHub Linguist to classify .ts files as TypeScript instead of JavaScript (the shebang line on build/cli.js and build/index.js currently misleads Linguist's heuristics).

    After all edits run the verification greps from the verification_greps block in context to confirm counts.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/larry/Documents/repos/momidala/prefect && grep -rE "prefect_|PREFECT_|@momidala/prefect|prefect-mcp" EXAMPLE_CLAUDE.md AGENTS.md CLAUDE.md examples/test-task.md examples/uat-v2.md 2>/dev/null | grep -v "\.config/prefect" | wc -l && cat .gitattributes</automated>
  </verify>
  <acceptance_criteria>
    - The verify command's first number (grep count excluding ~/.config/prefect path lines) equals 0
    - cat .gitattributes outputs exactly: *.ts linguist-language=TypeScript (with trailing newline)
    - .gitattributes file size is between 30 and 50 bytes (single line)
    - grep -c "legate_create_session" EXAMPLE_CLAUDE.md is >= 1
    - grep -c "LEGATE_" AGENTS.md is >= 1
    - grep -c "legate_" CLAUDE.md is >= 1
    - grep -c "legate_" examples/test-task.md is >= 1
    - grep -c "@momidala/legate" examples/uat-v2.md is >= 1
    - grep -c "Legate" CLAUDE.md is >= 1 (capitalized prose brand applied)
    - grep -rn "\.config/prefect" examples/uat-v2.md returns at least one line (preserved per out-of-scope rule)
    - No file in this task list contains the literal substring 'prefect-mcp' or 'prefect-update' anywhere
  </acceptance_criteria>
  <done>Five documentation files renamed to legate branding; .gitattributes created with TypeScript Linguist override; only ~/.config/prefect/ path references remain (intentional).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of README accuracy and migration flow</name>
  <what-built>
    Plans 01-03 together: code rename complete (Plans 01+02), tests all green, documentation rewritten (Plan 03 Tasks 1-2), .gitattributes created.
  </what-built>
  <how-to-verify>
    1. Open README.md in an editor. Confirm:
       - Install instructions show `npm install -g @momidala/legate` (not @momidala/prefect)
       - Tool examples reference `legate_*` (not prefect_*)
       - Env var examples reference `LEGATE_*` (not PREFECT_*)
       - There is a "Migrating from @momidala/prefect" section near the bottom with 5 clear steps
       - Migration section explicitly mentions that PREFECT_* env vars still work with a deprecation warning
       - Migration section explicitly mentions that ~/.config/prefect/ session data is preserved
    2. Run from project root: `grep -rn "prefect_\|PREFECT_" CLAUDE.md AGENTS.md EXAMPLE_CLAUDE.md examples/test-task.md examples/uat-v2.md` — expect zero matches (only ~/.config/prefect path references should remain, which the regex above does not match since it looks for the underscore form).
    3. Run from project root: `cat .gitattributes` — expect exactly one line: `*.ts linguist-language=TypeScript`.
    4. Optional: visually skim examples/uat-v2.md — confirm no stale prefect_ tool calls remain in the UAT steps.
    5. Run `npm test` once more from project root — confirm all tests still pass after the documentation pass (sanity check that no source file was accidentally touched).
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 5 documentation complete, or describe issues (e.g. "migration section missing step 4", "stale prefect_run reference at uat-v2.md line 87") for revision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> User | README and examples are the primary discovery surface; mis-documented install steps cause failed onboarding |
| .gitattributes -> GitHub | New file changes how GitHub classifies the repo's language stats and syntax highlighting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Information Disclosure | Documentation accidentally publishing secrets in env var examples | mitigate | Migration note uses generic placeholder values (e.g. `LEGATE_SERVER_PASSWORD=<your-password>`), never real credentials. Acceptance criteria do not require any specific credential value. |
| T-05-13 | Tampering | Stale install instructions could direct users to the deprecated @momidala/prefect package indefinitely | mitigate | Task 1 acceptance criteria require @momidala/legate in install command and zero @momidala/prefect references outside the migration section. Task 3 human checkpoint visually confirms install flow. |
| T-05-14 | Repudiation | Users skip the migration note and don't know what to do | accept | Heading 'Migrating from @momidala/prefect' is searchable; users with the old package name in their config will discover this section via grep / readme search. Migration is opt-in soft path per RENAME-04. |
| T-05-15 | Denial of Service | .gitattributes syntax error could break Linguist parsing for the entire repo | mitigate | Acceptance criterion requires exact line content '*.ts linguist-language=TypeScript' and file size 30-50 bytes (single line with newline). Standard Linguist syntax per RESEARCH.md citation. |
| T-05-16 | Spoofing | A stale `prefect-update.md` slash command file on user machines could still invoke `npm install -g @momidala/prefect` on the deprecated package | accept | Migration note step 2 explicitly tells users to `npm install -g @momidala/legate` which then installs the new `legate-update.md` slash command file (and the old prefect-update.md will sit unused; users can manually delete or accept the orphan). Out of scope per REQUIREMENTS.md "no alias/shim". |
</threat_model>

<verification>
- Task 1 acceptance criteria all met (README grep counts)
- Task 2 acceptance criteria all met (zero prefect-prefix tokens in five docs + .gitattributes correct)
- Task 3 human-verify checkpoint resumed with "approved" signal
- npm test still green after the documentation pass
</verification>

<success_criteria>
- DOC-01: All user-facing docs reference @momidala/legate, legate / legate-mcp binaries, legate_* tools, LEGATE_* env vars; README has a complete migration section
- DOC-02: .gitattributes exists at project root with the single Linguist override line
- All phase 5 requirements (RENAME-01..04 from Plans 01-02 + DOC-01..02 from this plan) are now satisfied
- Final human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/05-rename/05-03-SUMMARY.md` documenting:
- Final grep counts for each documentation file (prefect-prefix tokens outside ~/.config/prefect)
- Confirmation that .gitattributes file is exactly the expected single line
- Note from human checkpoint outcome (approval text or revisions applied)
- Phase 5 closeout note: all 6 requirements green; ready for /gsd-verify-work
</output>
