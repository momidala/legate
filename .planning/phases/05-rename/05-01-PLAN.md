---
phase: 05-rename
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/auth.test.ts
  - src/autostart.test.ts
  - src/sessions.test.ts
autonomous: true
requirements:
  - RENAME-04
user_setup: []
tags:
  - typescript
  - testing
  - env-vars
  - deprecation

must_haves:
  truths:
    - "auth.test.ts has a failing test asserting buildAuthHeader emits a one-time '[Legate] PREFECT_SERVER_PASSWORD is deprecated' warning on stderr when PREFECT_SERVER_PASSWORD is set and LEGATE_SERVER_PASSWORD is not"
    - "auth.test.ts has a failing test asserting buildAuthHeader emits a one-time '[Legate] PREFECT_SERVER_USERNAME is deprecated' warning when PREFECT_SERVER_USERNAME is set and LEGATE_SERVER_USERNAME is not"
    - "autostart.test.ts has a failing test asserting autostartTimeoutMs emits a one-time '[Legate] PREFECT_AUTOSTART_TIMEOUT_MS is deprecated' warning when only PREFECT_AUTOSTART_TIMEOUT_MS is set"
    - "sessions.test.ts has a failing test asserting readSessionMap emits a one-time '[Legate] PREFECT_SESSION_TTL_MS is deprecated' warning when only PREFECT_SESSION_TTL_MS is set"
    - "Each new test calls the function twice and asserts exactly one warning fires (one-time-per-process guard)"
    - "Tests fail with the current codebase (no LEGATE_* support yet) — proving RED state"
  artifacts:
    - path: "src/auth.test.ts"
      provides: "Deprecation warning coverage for PREFECT_SERVER_PASSWORD and PREFECT_SERVER_USERNAME"
      contains: "PREFECT_SERVER_PASSWORD is deprecated"
    - path: "src/autostart.test.ts"
      provides: "Deprecation warning coverage for PREFECT_AUTOSTART_TIMEOUT_MS"
      contains: "PREFECT_AUTOSTART_TIMEOUT_MS is deprecated"
    - path: "src/sessions.test.ts"
      provides: "Deprecation warning coverage for PREFECT_SESSION_TTL_MS"
      contains: "PREFECT_SESSION_TTL_MS is deprecated"
  key_links:
    - from: "src/auth.test.ts"
      to: "src/auth.ts"
      via: "imports buildAuthHeader and _resetWarnFlags"
      pattern: "from './auth"
    - from: "src/autostart.test.ts"
      to: "src/autostart.ts"
      via: "calls autostart entry that reads timeout env var"
      pattern: "from './autostart"
    - from: "src/sessions.test.ts"
      to: "src/sessions.ts"
      via: "imports readSessionMap"
      pattern: "from './sessions"
---

<objective>
Add failing deprecation-warning tests for the LEGATE_* / PREFECT_* / OPENCODE_* env var chain to lock in the Wave 0 contract before any source rename in Plan 02.

Purpose: RENAME-04 requires a soft migration — read both LEGATE_* and PREFECT_*, prefer LEGATE_*, warn once on PREFECT_*. The current codebase has no LEGATE_* support and no PREFECT_* deprecation warnings. These tests must exist and fail FIRST so Plan 02's implementation has a concrete acceptance contract.

Output: Three test files updated with new deprecation tests; all new tests RED against current code; existing tests still green (no regressions in unrelated paths).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-rename/05-RESEARCH.md
@.planning/phases/05-rename/05-PATTERNS.md
@.planning/phases/05-rename/05-VALIDATION.md

<interfaces>
<!-- Key signatures extracted from current src/auth.ts, src/autostart.ts, src/sessions.ts -->
<!-- Executor uses these signatures directly — no file exploration needed for test wiring -->

From src/auth.ts (current):
- export function buildAuthHeader(): Record<string, string>
- export function _resetWarnFlags(): void   // existing helper — currently resets warnedPassword, warnedUsername
- Reads: PREFECT_SERVER_PASSWORD (primary), OPENCODE_SERVER_PASSWORD (deprecated tier with warning)
- Reads: PREFECT_SERVER_USERNAME (primary), OPENCODE_SERVER_USERNAME (deprecated tier with warning)
- Warning text shape: '[Prefect] OPENCODE_SERVER_PASSWORD is deprecated, use PREFECT_SERVER_PASSWORD'
- After Plan 02 rename, primary becomes LEGATE_*, with PREFECT_* warning text: '[Legate] PREFECT_SERVER_PASSWORD is deprecated, use LEGATE_SERVER_PASSWORD'

From src/autostart.ts (current):
- function autostartTimeoutMs(): number — module-private, reads PREFECT_AUTOSTART_TIMEOUT_MS directly with no warning
- Plan 02 target: also export _resetWarnFlags from autostart for test isolation, OR expose a test helper hook similar to auth.ts pattern

From src/sessions.ts (current):
- export function readSessionMap(path: string): SessionMap (reads PREFECT_SESSION_TTL_MS inline at call time)
- Plan 02 target: add module-level warnedSessionTtl flag; same module-level reset helper pattern

Existing test pattern (src/auth.test.ts lines 17-29): env var save/restore via try/finally
Existing test pattern: tests call _resetWarnFlags() before exercising one-time warning behavior

Warning expectation contract (RENAME-04):
- One-time per process per env var
- Emits to console.error (stderr, NOT stdout — MCP protocol uses stdout)
- Warning text format: '[Legate] <OLD_VAR_NAME> is deprecated, use <NEW_VAR_NAME>'
- Setting LEGATE_* alongside PREFECT_* must NOT emit the deprecation warning (LEGATE_* takes precedence)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PREFECT_SERVER_PASSWORD and PREFECT_SERVER_USERNAME deprecation tests to auth.test.ts</name>
  <files>src/auth.test.ts</files>
  <read_first>
    - src/auth.test.ts (current env-var save/restore patterns lines 17-29; existing OPENCODE_ deprecation test structure if any)
    - src/auth.ts (current buildAuthHeader behavior; current module-level warn flags; _resetWarnFlags signature)
    - .planning/phases/05-rename/05-PATTERNS.md (section "src/auth.test.ts (test)" template for deprecation warning tests)
    - .planning/phases/05-rename/05-RESEARCH.md ("Three-Tier Fallback Chain Complexity" pitfall and "Deprecation Warning Pattern" example)
  </read_first>
  <behavior>
    - Test 1: buildAuthHeader warns once with text including 'PREFECT_SERVER_PASSWORD is deprecated' and 'LEGATE_SERVER_PASSWORD' when only PREFECT_SERVER_PASSWORD is set in env
    - Test 2: calling buildAuthHeader twice in the same test with PREFECT_SERVER_PASSWORD set emits exactly one warning (one-time-per-process guard)
    - Test 3: setting both LEGATE_SERVER_PASSWORD and PREFECT_SERVER_PASSWORD emits NO deprecation warning (LEGATE_* takes precedence)
    - Test 4: identical three tests for PREFECT_SERVER_USERNAME / LEGATE_SERVER_USERNAME
    - Each test deletes LEGATE_*/PREFECT_*/OPENCODE_* env vars in setup/teardown to isolate from process env
    - Each test calls _resetWarnFlags() before exercising (so order does not matter)
    - Warning text matched by substring (not exact equality) so brand prefix change tolerant — assertion uses indexOf or .includes on the deprecated var name
  </behavior>
  <action>
    Append four new test cases to src/auth.test.ts using the existing node:test (test, assert) imports already at the top of the file. Mirror the env-var save/restore try/finally pattern from existing tests in this file. Use the console.error capture pattern from 05-PATTERNS.md ("New Deprecation Warning Tests" template): save the original console.error reference, replace with a function that pushes args into a local string array, run the function under test, restore in finally. Reset warn flags using _resetWarnFlags() at the start of each new test. Use the constant warning substrings 'PREFECT_SERVER_PASSWORD' and 'PREFECT_SERVER_USERNAME' for assertion (matching the warning text Plan 02 will emit per RESEARCH.md "Three-Tier Env Var Deprecation Pattern"). For the LEGATE-takes-precedence test, set both LEGATE_SERVER_PASSWORD='new' and PREFECT_SERVER_PASSWORD='old' and assert warnings.filter(w => w.includes('PREFECT_SERVER_PASSWORD')).length === 0. Do NOT modify any other existing tests. Do NOT add LEGATE_* support to src/auth.ts — that is Plan 02's responsibility; these tests MUST fail against the current source.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/larry/Documents/repos/momidala/prefect && npm test 2>&1 | grep -E "(PREFECT_SERVER_PASSWORD|PREFECT_SERVER_USERNAME).*deprecated" | grep -E "(fail|not ok)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "PREFECT_SERVER_PASSWORD is deprecated" src/auth.test.ts >= 1
    - grep -c "PREFECT_SERVER_USERNAME is deprecated" src/auth.test.ts >= 1
    - grep -c "LEGATE_SERVER_PASSWORD" src/auth.test.ts >= 2
    - grep -c "LEGATE_SERVER_USERNAME" src/auth.test.ts >= 2
    - grep -c "_resetWarnFlags" src/auth.test.ts >= 4 (at least one call per new test)
    - `npm test` shows new auth.test.ts tests FAILING with output mentioning the deprecation strings (RED state confirmed before Plan 02 runs)
    - Pre-existing tests in auth.test.ts that do NOT touch the new behavior still pass
    - No new exports added to src/auth.ts in this plan (file unchanged)
  </acceptance_criteria>
  <done>auth.test.ts contains four new tests covering PREFECT_SERVER_PASSWORD and PREFECT_SERVER_USERNAME deprecation contract; new tests fail against current source; existing tests unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add PREFECT_AUTOSTART_TIMEOUT_MS and PREFECT_SESSION_TTL_MS deprecation tests</name>
  <files>src/autostart.test.ts, src/sessions.test.ts</files>
  <read_first>
    - src/autostart.test.ts (current PREFECT_AUTOSTART_TIMEOUT_MS handling lines 65-66, 75-76; existing test patterns)
    - src/sessions.test.ts (current PREFECT_SESSION_TTL_MS handling lines 171-208; temp dir setup pattern line 9)
    - src/autostart.ts (current autostartTimeoutMs implementation; where to call from a test if it is module-private)
    - src/sessions.ts (current readSessionMap signature line 33 inline env read)
    - .planning/phases/05-rename/05-PATTERNS.md (sections "src/autostart.test.ts" and "src/sessions.test.ts")
  </read_first>
  <behavior>
    - autostart.test.ts: new test asserts that whatever public surface calls autostartTimeoutMs() (likely a higher-level autostart function in autostart.ts) emits one-time '[Legate] PREFECT_AUTOSTART_TIMEOUT_MS is deprecated, use LEGATE_AUTOSTART_TIMEOUT_MS' warning when only PREFECT_AUTOSTART_TIMEOUT_MS is set
    - autostart.test.ts: setting both LEGATE_AUTOSTART_TIMEOUT_MS and PREFECT_AUTOSTART_TIMEOUT_MS emits NO warning
    - autostart.test.ts: function called twice yields exactly one warning (one-time-per-process)
    - sessions.test.ts: readSessionMap emits one-time '[Legate] PREFECT_SESSION_TTL_MS is deprecated, use LEGATE_SESSION_TTL_MS' warning when only PREFECT_SESSION_TTL_MS is set
    - sessions.test.ts: setting both LEGATE_SESSION_TTL_MS and PREFECT_SESSION_TTL_MS emits NO warning
    - sessions.test.ts: readSessionMap called twice yields exactly one warning
    - Each test cleans up env vars in try/finally; each test uses a fresh temp dir for sessions.test.ts
  </behavior>
  <action>
    In src/autostart.test.ts append three new tests. Since autostartTimeoutMs is module-private in src/autostart.ts per PATTERNS.md, Plan 02 will either export it directly or expose a _resetWarnFlags() helper analogous to auth.ts. For now, write the tests assuming Plan 02 will export a function autostartTimeoutMs() or equivalent helper from src/autostart.ts that returns the resolved number — annotate with a comment '// expects Plan 02 to export autostartTimeoutMs and _resetWarnFlags from autostart.ts'. Use the same console.error capture pattern as Task 1. Assert warnings.filter(w => w.includes('PREFECT_AUTOSTART_TIMEOUT_MS')).length === 1 after two calls.

    In src/sessions.test.ts append three new tests for readSessionMap (already exported). Create a fresh temp dir per test using the existing mkdtemp pattern (line 9 prefix). Use the same console.error capture pattern. Assert one-time warning. For the precedence test, set LEGATE_SESSION_TTL_MS='2000' and PREFECT_SESSION_TTL_MS='1000', call readSessionMap, assert no deprecation warning fired AND that the resolved TTL came from LEGATE_* (use the function's behavioral output if observable, otherwise just assert no warning).

    Do NOT modify autostart.ts or sessions.ts — Plan 02 owns the implementation. Tests MUST fail RED against current source.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/larry/Documents/repos/momidala/prefect && npm test 2>&1 | grep -E "(PREFECT_AUTOSTART_TIMEOUT_MS|PREFECT_SESSION_TTL_MS).*deprecated" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "PREFECT_AUTOSTART_TIMEOUT_MS is deprecated" src/autostart.test.ts >= 1
    - grep -c "LEGATE_AUTOSTART_TIMEOUT_MS" src/autostart.test.ts >= 2
    - grep -c "PREFECT_SESSION_TTL_MS is deprecated" src/sessions.test.ts >= 1
    - grep -c "LEGATE_SESSION_TTL_MS" src/sessions.test.ts >= 2
    - `npm test` shows the new autostart.test.ts and sessions.test.ts deprecation tests FAILING (compilation may fail because autostartTimeoutMs is not exported; that is the expected RED state)
    - Pre-existing tests not touching new behavior remain unchanged in expectations (no diff to existing test bodies beyond appended cases)
    - src/autostart.ts unchanged
    - src/sessions.ts unchanged
  </acceptance_criteria>
  <done>autostart.test.ts and sessions.test.ts each contain three new tests covering the deprecation contract; tests fail against current source; existing tests not modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env → MCP server | Untrusted env vars (LEGATE_*, PREFECT_*, OPENCODE_*) cross from user shell into Node process at module load |
| stderr → MCP host | Deprecation warning text crosses into Claude Code's MCP log stream; must not leak secret values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | console.error deprecation warning text in auth.ts | mitigate | Tests assert warning text contains only env var NAMES (e.g. 'PREFECT_SERVER_PASSWORD'), never values. The Task 1/2 assertions use substring matches on var names; if a future implementation logs the value, tests still pass but the warning is wrong — relies on Plan 02 implementer following 05-PATTERNS.md pattern which logs name only. |
| T-05-02 | Tampering | env var precedence (LEGATE_ vs PREFECT_) | mitigate | Tests in this plan explicitly cover the LEGATE-takes-precedence case so a future bug where PREFECT_* overrides LEGATE_* is caught at npm test time. |
| T-05-03 | Repudiation | one-time-warn guard could mask repeated misuse | accept | One-time warning is intentional UX (RENAME-04 requirement); operators can grep stderr once. Solo dev project, no audit requirement. |
| T-05-04 | Denial of Service | n/a | accept | Test additions are pure-CPU, no external calls. |
</threat_model>

<verification>
- Run `npm test` from project root
- New deprecation tests in auth.test.ts, autostart.test.ts, sessions.test.ts MUST be RED (failing)
- All other pre-existing tests MUST remain GREEN
- TypeScript compile must succeed for the test files themselves (even if test assertions fail at runtime); if Task 2 references autostartTimeoutMs that is currently not exported, compilation will fail — that compilation failure IS the documented RED state for Plan 02 to fix
- No changes to any non-test file in this plan
</verification>

<success_criteria>
- auth.test.ts contains 4 new tests covering RENAME-04 PASSWORD/USERNAME deprecation contract
- autostart.test.ts contains 3 new tests covering RENAME-04 AUTOSTART_TIMEOUT_MS deprecation contract
- sessions.test.ts contains 3 new tests covering RENAME-04 SESSION_TTL_MS deprecation contract
- All new tests RED against current source (npm test output shows failures referencing PREFECT_*/LEGATE_* strings)
- No source files (non-test) modified in this plan
- git diff scoped to src/auth.test.ts, src/autostart.test.ts, src/sessions.test.ts only
</success_criteria>

<output>
After completion, create `.planning/phases/05-rename/05-01-SUMMARY.md` documenting:
- Which tests were added in each file (with line numbers post-edit)
- The RED command output proving the tests fail against current source
- Note for Plan 02: expected export from src/autostart.ts (autostartTimeoutMs or equivalent test hook)
</output>
