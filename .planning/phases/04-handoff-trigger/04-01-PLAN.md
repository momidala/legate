---
phase: 04-handoff-trigger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - CKPT-03
tags:
  - tracking
  - verification
  - handoff
  - ckpt-03

must_haves:
  truths:
    - "AGENTS.md ## Checkpointing section contains the calibrated trigger phrase 'conversation feels crowded'"
    - "AGENTS.md ## Checkpointing section contains the explicit stop instruction 'stop initiating new work in this session. Do not wait for an error.'"
    - "AGENTS.md ## Checkpointing section is the final ## section in the file (trigger lives where agents will encounter it)"
    - "REQUIREMENTS.md shows CKPT-03 as checked ([x]) on the requirement bullet"
    - "REQUIREMENTS.md traceability table shows CKPT-03 with Status = Complete"
    - "ROADMAP.md Phase 4 plan list contains [x] 04-01-PLAN.md entry"
    - "ROADMAP.md Progress table shows Phase 4 row as 1/1 Complete with date 2026-05-12"
    - "STATE.md YAML front matter shows completed_phases=4, completed_plans=5, percent=100, status=complete"
    - "STATE.md Current Position reflects Phase 4 complete (100% bar)"
    - "STATE.md Blockers/Concerns no longer references the resolved Phase 1 context API blocker"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CKPT-03 closure (bullet checked + traceability row Complete + datestamp updated)"
      contains: "- [x] **CKPT-03**"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 plan list entry and progress row marked Complete"
      contains: "| 4. Handoff Trigger | 1/1 | Complete | 2026-05-12 |"
    - path: ".planning/STATE.md"
      provides: "Milestone-complete project state (4/4 phases, 5/5 plans, 100%)"
      contains: "percent: 100"
  key_links:
    - from: "AGENTS.md ## Checkpointing"
      to: "CKPT-03 satisfaction"
      via: "instructed self-detection (Phase 1 D-02) auto-loaded by OpenCode from session directory (Phase 1 Finding 1)"
      pattern: "conversation feels crowded.*stop initiating new work"
    - from: ".planning/REQUIREMENTS.md CKPT-03 row"
      to: ".planning/ROADMAP.md Phase 4 progress row"
      via: "consistent 'Complete' vocabulary across both tracking surfaces"
      pattern: "CKPT-03.*Complete"
    - from: ".planning/STATE.md progress counters"
      to: "milestone v2.1 100% complete"
      via: "YAML front matter mirrors prose Current Position section"
      pattern: "completed_phases: 4"
---

<objective>
Close CKPT-03 by (a) verifying the Handoff trigger instruction is live and well-formed in `AGENTS.md` (delivered by Phase 3) and (b) updating the three planning tracking artifacts (REQUIREMENTS.md, ROADMAP.md, STATE.md) to record CKPT-03 as satisfied and the v2.1 milestone as complete.

Purpose: Per Phase 4 RESEARCH.md, the trigger mechanism (instructed self-detection in AGENTS.md) was delivered in Phase 3. Phase 4 is a verification + tracking close-out — NOT a code phase. RESEARCH Pitfall 1 explicitly warns: "any task that modifies a `.ts` file is a plan error." Phase 4 fully closes the v2.1 milestone by recording CKPT-03 as Complete.

Output: Verified AGENTS.md trigger; updated REQUIREMENTS.md (CKPT-03 bullet checked, traceability row Complete, footer datestamp); updated ROADMAP.md (Phase 4 plan list and progress row Complete); updated STATE.md (progress counters at 100%, blocker resolved).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-handoff-trigger/04-RESEARCH.md
@.planning/phases/04-handoff-trigger/04-PATTERNS.md
@.planning/phases/04-handoff-trigger/04-VALIDATION.md

<!--
RESEARCH.md ground truth (cite when executing):
- Phase 4 is a verification + tracking close-out. No source code changes.
- The trigger instruction is already in AGENTS.md lines 102–116 (from Phase 3).
- Trigger uses instructed self-detection (Phase 1 D-02); no API for context %.
- CKPT-01, CKPT-02, CKPT-04 tracking and Phase 3 progress row staleness are OUT OF SCOPE for Phase 4
  (per planning_context deep_work_rules). Touch only the CKPT-03 / Phase-4 rows.
-->

<interfaces>
<!-- These are the exact source-of-truth strings the executor must match verbatim. -->

AGENTS.md ## Checkpointing (lines 102–116) — already present from Phase 3, MUST NOT be modified:
  - Trigger phrase (calibrated, do not paraphrase):
    "if you have been working for a long time, if tracking all state feels difficult, or if the conversation feels crowded"
  - Stop instruction:
    "stop initiating new work in this session. Do not wait for an error."
  - Handoff schema fields: accomplished, current_state, next_steps, open_questions

REQUIREMENTS.md CKPT-03 bullet (line 22 — current state):
  "- [ ] **CKPT-03**: When a prefect agent's context reaches ~80%, the agent writes `Handoff.md` and stops work (trigger mechanism: researched in CKPT-05)"

REQUIREMENTS.md CKPT-03 traceability row (line 58 — current state):
  "| CKPT-03 | Phase 4 | Pending |"

REQUIREMENTS.md footer (line 67 — current state):
  "*Last updated: 2026-05-11 — roadmap created, all 10 requirements mapped*"

ROADMAP.md Phase 4 Plans line (line 76 — current state):
  "**Plans**: TBD"

ROADMAP.md Phase 4 Progress row (line 89 — current state):
  "| 4. Handoff Trigger | 0/TBD | Not started | - |"

STATE.md YAML progress block (lines 9–14 — current state):
  total_phases: 4
  completed_phases: 3
  total_plans: 4
  completed_plans: 3
  percent: 75

STATE.md status/stopped_at fields (lines 5–8 — current state):
  status: ready_to_plan
  stopped_at: Phase 2 context gathered
  last_activity: 2026-05-12 -- Phase 03 execution started

STATE.md Current Position block (lines 27–33 — current state):
  Phase: 4 / Plan: Not started / Status: Ready to plan / Progress: [░░░░░░░░░░] 0%

STATE.md Blockers/Concerns (lines 67–69 — current state, now stale):
  "OpenCode context utilization API is unknown — Phase 1 research spike must complete before Phase 4 can be planned"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify CKPT-03 trigger is live in AGENTS.md</name>
  <files>AGENTS.md (read-only verification — no modification)</files>
  <read_first>
    - AGENTS.md (the file being audited)
    - .planning/phases/04-handoff-trigger/04-RESEARCH.md (Code Examples section: exact grep commands; Pitfall 1: do not modify .ts files; Anti-Patterns: do not re-wire the trigger)
    - .planning/phases/04-handoff-trigger/04-VALIDATION.md (Per-Task Verification Map: the canonical grep command)
  </read_first>
  <action>
    Verification-only task — read AGENTS.md and confirm the Handoff trigger instruction delivered by Phase 3 is present and unmodified. AGENTS.md MUST NOT be edited in this task. Run the four grep audit commands from RESEARCH.md Code Examples (each must exit 0) and record their outcome in the task report:

    1. `grep -F "if you have been working for a long time, if tracking all state feels difficult, or if the conversation feels crowded" AGENTS.md` — confirms the calibrated trigger phrase (exact wording, do not accept paraphrases).
    2. `grep -F "stop initiating new work in this session. Do not wait for an error." AGENTS.md` — confirms the stop instruction.
    3. `grep -qF "accomplished" AGENTS.md && grep -qF "current_state" AGENTS.md && grep -qF "next_steps" AGENTS.md && grep -qF "open_questions" AGENTS.md` — confirms all four Handoff schema fields.
    4. `grep -n "^## " AGENTS.md | tail -1` — confirms `## Checkpointing` is the LAST `##` section in the file (trigger lives at the end where agents will encounter it).

    If any grep fails (non-zero exit) or command 4's output does not end with `## Checkpointing`, STOP and report the failure — Task 2 must not run. Phase 4 would then require a corrective amendment to AGENTS.md (out of the originally planned scope; surface to the user).
  </action>
  <verify>
    <automated>grep -F "if you have been working for a long time, if tracking all state feels difficult, or if the conversation feels crowded" AGENTS.md &amp;&amp; grep -F "stop initiating new work in this session. Do not wait for an error." AGENTS.md &amp;&amp; grep -qF "accomplished" AGENTS.md &amp;&amp; grep -qF "current_state" AGENTS.md &amp;&amp; grep -qF "next_steps" AGENTS.md &amp;&amp; grep -qF "open_questions" AGENTS.md &amp;&amp; [ "$(grep -n '^## ' AGENTS.md | tail -1 | sed 's/.*## //')" = "Checkpointing" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "if you have been working for a long time, if tracking all state feels difficult, or if the conversation feels crowded" AGENTS.md` exits 0
    - `grep -F "stop initiating new work in this session. Do not wait for an error." AGENTS.md` exits 0
    - `grep -qF "accomplished" AGENTS.md` exits 0
    - `grep -qF "current_state" AGENTS.md` exits 0
    - `grep -qF "next_steps" AGENTS.md` exits 0
    - `grep -qF "open_questions" AGENTS.md` exits 0
    - The output of `grep -n "^## " AGENTS.md | tail -1` ends with the string `## Checkpointing`
    - `git status` shows AGENTS.md UNMODIFIED after this task (no edits were made — verification only)
  </acceptance_criteria>
  <done>All four grep audits pass; AGENTS.md is unmodified by this task; trigger phrase, stop instruction, Handoff schema fields, and section placement are all confirmed live; the executor records the four passing grep results in the task report and proceeds to Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Close CKPT-03 tracking in REQUIREMENTS.md, ROADMAP.md, STATE.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state of CKPT-03 bullet line 22, traceability row line 58, footer line 67)
    - .planning/ROADMAP.md (current state of Phase 4 Plans line 76, Progress row line 89)
    - .planning/STATE.md (current YAML lines 1–15, Current Position block lines 27–33, Blockers section lines 67–69)
    - .planning/phases/04-handoff-trigger/04-PATTERNS.md (exact target strings for every edit; analog patterns from Phase 1 rows already marked Complete)
    - .planning/phases/04-handoff-trigger/04-RESEARCH.md (deep_work_rules constraint: only Phase 4 / CKPT-03 rows are in scope — do NOT touch CKPT-01/02/04 bullets, do NOT touch Phase 3 progress row, do NOT touch SELFUP rows)
  </read_first>
  <action>
    Apply the seven scoped edits below. Touch ONLY the lines specified. Preserve every other line verbatim. Do NOT modify CKPT-01, CKPT-02, CKPT-04 bullets, the Phase 3 progress row, or any SELFUP rows — those are explicitly out of Phase 4 scope per the planning_context deep_work_rules. Do NOT modify AGENTS.md.

    EDIT 1 — .planning/REQUIREMENTS.md line 22 (CKPT-03 bullet checkbox flip):
      Change leading `- [ ]` to `- [x]` on the CKPT-03 bullet. Preserve the rest of the line verbatim, including the bold `**CKPT-03**`, the colon, the requirement description text, and the parenthetical "(trigger mechanism: researched in CKPT-05)". Per 04-PATTERNS.md "GFM Checkbox flip pattern" — bracket content is the only change.

    EDIT 2 — .planning/REQUIREMENTS.md line 58 (CKPT-03 traceability row):
      In the row matching `| CKPT-03 | Phase 4 | ... |`, change the Status cell from `Pending` to `Complete`. Title-case, no trailing punctuation (matches the vocabulary in ROADMAP.md Progress table per 04-PATTERNS.md "Traceability table status pattern"). Touch no other rows.

    EDIT 3 — .planning/REQUIREMENTS.md line 67 (footer datestamp):
      Replace the footer line `*Last updated: 2026-05-11 — roadmap created, all 10 requirements mapped*` with `*Last updated: 2026-05-12 — CKPT-03 marked complete (Phase 4)*`.

    EDIT 4 — .planning/ROADMAP.md line 76 (Phase 4 Plans block):
      Replace the single line `**Plans**: TBD` with three lines:
        Line A: `**Plans:** 1 plan`
        Line B: `Plans:`
        Line C: `- [x] 04-01-PLAN.md — Verify CKPT-03 trigger is live in AGENTS.md; close CKPT-03 tracking entries in REQUIREMENTS.md, ROADMAP.md, STATE.md`
      Format must match the analog Phase 1 Plans block (ROADMAP.md lines 32–34) — `**Plans:**` uses a colon-inside-bold style, the literal `Plans:` header line, then a `- [x]` task-list entry per the "GFM Checkbox flip pattern" in 04-PATTERNS.md.

    EDIT 5 — .planning/ROADMAP.md line 89 (Phase 4 Progress row):
      In the Progress table, replace the Phase 4 row `| 4. Handoff Trigger | 0/TBD | Not started | - |` with `| 4. Handoff Trigger | 1/1 | Complete | 2026-05-12 |`. Format must match the analog Phase 1 row (ROADMAP.md line 86) per 04-PATTERNS.md "Progress table completion row pattern". Do NOT modify Phase 3's progress row (out of Phase 4 scope).

    EDIT 6 — .planning/STATE.md YAML front matter (lines 5–14):
      Update the YAML front matter as follows. Preserve `gsd_state_version`, `milestone`, and `milestone_name` exactly:
        - `status:` change value from `ready_to_plan` to `complete`
        - `stopped_at:` change value from `Phase 2 context gathered` to `Phase 4 complete`
        - `last_updated:` set to an ISO-8601 UTC timestamp for today (date 2026-05-12 — use the value `"2026-05-12T00:00:00.000Z"` if a precise timestamp is not otherwise available)
        - `last_activity:` change to `2026-05-12 -- Phase 04 complete`
        - In the `progress:` block: `completed_phases` from 3 to 4; `total_plans` from 4 to 5; `completed_plans` from 4 to 5; `percent` from 75 to 100. Keep `total_phases: 4` unchanged.

    EDIT 7 — .planning/STATE.md Current Position block (lines 27–33):
      Replace the block with:
        Phase: 4
        Plan: 04-01-PLAN.md (complete)
        Status: Complete
        Last activity: 2026-05-12

        Progress: [██████████] 100%
      Preserve the leading `## Current Position` heading and the blank line that follows it.

    EDIT 8 — .planning/STATE.md Blockers/Concerns section (lines 67–69):
      Replace the stale blocker bullet (`- OpenCode context utilization API is unknown — Phase 1 research spike must complete before Phase 4 can be planned`) with the single line `None — all phases complete.`. Preserve the `### Blockers/Concerns` heading and the blank line that follows it.

    After all edits, run `git diff -- .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md` and visually confirm the diff matches exactly the edits described above — no other lines should appear in the diff.
  </action>
  <verify>
    <automated>grep -qF "- [x] **CKPT-03**" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "^\| CKPT-03 \| Phase 4 \| Complete \|" .planning/REQUIREMENTS.md &amp;&amp; grep -qF "Last updated: 2026-05-12 — CKPT-03 marked complete (Phase 4)" .planning/REQUIREMENTS.md &amp;&amp; grep -qF "04-01-PLAN.md — Verify CKPT-03 trigger is live in AGENTS.md" .planning/ROADMAP.md &amp;&amp; grep -qE "^\| 4\. Handoff Trigger \| 1/1 \| Complete \| 2026-05-12 \|" .planning/ROADMAP.md &amp;&amp; grep -qE "^status: complete$" .planning/STATE.md &amp;&amp; grep -qE "^  completed_phases: 4$" .planning/STATE.md &amp;&amp; grep -qE "^  total_plans: 5$" .planning/STATE.md &amp;&amp; grep -qE "^  completed_plans: 5$" .planning/STATE.md &amp;&amp; grep -qE "^  percent: 100$" .planning/STATE.md &amp;&amp; grep -qF "Progress: [██████████] 100%" .planning/STATE.md &amp;&amp; grep -qF "None — all phases complete." .planning/STATE.md &amp;&amp; ! grep -qF "OpenCode context utilization API is unknown" .planning/STATE.md &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md contains a line matching `- [x] **CKPT-03**: When a prefect agent's context reaches ~80%` (bullet flipped, description preserved verbatim)
    - .planning/REQUIREMENTS.md still contains `- [ ] **CKPT-01**`, `- [ ] **CKPT-02**`, `- [ ] **CKPT-04**` UNCHANGED (out of Phase 4 scope — they remain `[ ]`)
    - .planning/REQUIREMENTS.md traceability table row matches `| CKPT-03 | Phase 4 | Complete |` exactly
    - .planning/REQUIREMENTS.md traceability rows for CKPT-01, CKPT-02, CKPT-04, CKPT-05, and all SELFUP-* are UNCHANGED (still show prior status text)
    - .planning/REQUIREMENTS.md footer reads `*Last updated: 2026-05-12 — CKPT-03 marked complete (Phase 4)*`
    - .planning/ROADMAP.md Phase 4 block contains the three lines `**Plans:** 1 plan`, `Plans:`, and `- [x] 04-01-PLAN.md — Verify CKPT-03 trigger is live in AGENTS.md; close CKPT-03 tracking entries in REQUIREMENTS.md, ROADMAP.md, STATE.md`
    - .planning/ROADMAP.md no longer contains the string `**Plans**: TBD`
    - .planning/ROADMAP.md Progress table contains the row `| 4. Handoff Trigger | 1/1 | Complete | 2026-05-12 |` exactly
    - .planning/ROADMAP.md Phase 3 Progress row `| 3. Checkpoint Schemas + AGENTS.md | 0/1 | Not started | - |` is UNCHANGED (out of Phase 4 scope)
    - .planning/STATE.md YAML front matter contains all of: `status: complete`, `stopped_at: Phase 4 complete`, `last_activity: 2026-05-12 -- Phase 04 complete`, `completed_phases: 4`, `total_plans: 5`, `completed_plans: 5`, `percent: 100`, `total_phases: 4`
    - .planning/STATE.md `last_updated` field is an ISO-8601 UTC timestamp for 2026-05-12 (value `"2026-05-12T00:00:00.000Z"` acceptable)
    - .planning/STATE.md Current Position block contains the lines `Phase: 4`, `Plan: 04-01-PLAN.md (complete)`, `Status: Complete`, `Last activity: 2026-05-12`, and `Progress: [██████████] 100%`
    - .planning/STATE.md Blockers/Concerns section contains `None — all phases complete.` and does NOT contain `OpenCode context utilization API is unknown`
    - `git status` shows ONLY `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` modified (plus the new plan file). No `src/`, no `AGENTS.md`, no other tracking files touched.
    - `npm test` exits 0 (regression guard — no source changed, but confirms nothing broke)
  </acceptance_criteria>
  <done>All three planning tracking files reflect Phase 4 / CKPT-03 closure; out-of-scope rows (CKPT-01/02/04, Phase 3 progress) remain untouched; no source code modified; `npm test` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Phase 4 modifies only `.planning/*.md` tracking artifacts inside the developer's own repo. No new endpoints, no new code paths, no new external data ingress, no auth surfaces, no file-system access patterns beyond standard markdown edits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `.planning/REQUIREMENTS.md` traceability table | accept | Tracking artifacts are checked into version control and reviewed via PR/`git diff`. Task 2 verify step asserts the exact target row with anchored grep (`^\| CKPT-03 \| Phase 4 \| Complete \|`) and the acceptance criteria require unrelated rows to remain unchanged — any out-of-scope mutation surfaces in `git diff`. Low-value target (no secrets, no PII, no runtime behavior). |
| T-04-02 | Information Disclosure | `.planning/STATE.md` YAML front matter | accept | YAML contains only milestone progress counters and timestamps. No credentials, tokens, or PII. The file is already public in the project repository. ASVS V8 does not apply. |
| T-04-03 | Repudiation | Phase 4 tracking edits | accept | All edits land via git commits with author identity and timestamp (standard CLAUDE.md git workflow). Audit trail is the commit history. No additional logging needed. |
| T-04-04 | Tampering (scope creep) | AGENTS.md modified by mistake | mitigate | Task 1 acceptance criterion explicitly asserts `git status` shows AGENTS.md UNMODIFIED after verification. Task 2 acceptance criterion explicitly asserts only the three planning files are in `git status`. Plus RESEARCH Pitfall 1 ("Any task that modifies a `.ts` file is a plan error") and the in-task notes warn against scope drift. |

**ASVS L1 mapping:** V1 (architecture) — no new components introduced. V2–V14 not applicable: no auth, no session management, no input handling, no crypto, no errors/logging changes, no data protection, no comms, no business logic, no files/resources, no APIs, no config, no malicious code, no internal sec. All "accept" dispositions justified by zero net change to the attack surface.

**High severity threats:** None. No mitigation gating required.
</threat_model>

<verification>
1. Task 1 grep audit passes (AGENTS.md trigger verified live and unmodified)
2. Task 2 leaves only `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, and the new plan file in `git status` (no source code modified, no AGENTS.md modified, no out-of-scope rows changed)
3. All `acceptance_criteria` greps in Task 2's `<automated>` block exit 0
4. `npm test` exits 0 (regression guard: source unchanged, build untouched)
5. After commit, `git log -1 --stat` shows only the four expected files
</verification>

<success_criteria>
- AGENTS.md trigger phrase + stop instruction + Handoff schema fields confirmed present (Task 1 grep audit, all four commands exit 0)
- AGENTS.md `## Checkpointing` is the final `##` section
- REQUIREMENTS.md: CKPT-03 bullet checked, traceability row `Complete`, footer datestamp updated
- REQUIREMENTS.md: CKPT-01/CKPT-02/CKPT-04 bullets unchanged (out of Phase 4 scope per deep_work_rules)
- ROADMAP.md: Phase 4 Plans block populated, Progress row `1/1 | Complete | 2026-05-12`
- ROADMAP.md: Phase 3 Progress row unchanged (out of Phase 4 scope)
- STATE.md: YAML counters at 4/4 phases, 5/5 plans, 100%, `status: complete`, blocker resolved
- `npm test` exits 0
- `git status` shows exactly four modified/added files
</success_criteria>

<output>
After completion, create `.planning/phases/04-handoff-trigger/04-01-SUMMARY.md` per the GSD summary template, including:
- Tasks completed (Task 1 verification result + Task 2 edit list)
- The four grep outputs from Task 1 (proof of CKPT-03 trigger live in AGENTS.md)
- `git diff --stat` of the three tracking files
- `requirements-completed: [CKPT-03]`
- Note that v2.1 milestone is now 100% complete
</output>
