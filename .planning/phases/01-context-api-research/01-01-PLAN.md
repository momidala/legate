---
phase: 01-context-api-research
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/phase-1-findings.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - CKPT-05
must_haves:
  truths:
    - "Phase 3 implementer can read .planning/research/phase-1-findings.md alone and know which delivery mechanism to use (AGENTS.md primary, per-run system backup) without reading CONTEXT.md or RESEARCH.md"
    - "Phase 4 implementer can read .planning/research/phase-1-findings.md alone and know to use instructed self-detection (no API trigger available) without re-investigating"
    - "PROJECT.md Key Decisions table no longer says 'Pending' for the 'Context % as handoff trigger' row — it reflects the actual finding (instructed self-detection per D-02)"
    - "PROJECT.md Context section no longer says 'delivery mechanism is unknown' or 'OpenCode context utilization API: unknown' — both are answered"
    - "ROADMAP.md Phase 3 and Phase 4 entries link to .planning/research/phase-1-findings.md so future planners do not re-investigate"
    - "The canonical AGENTS.md checkpoint instruction template (verbatim text Phase 3 will paste) appears in the findings doc"
  artifacts:
    - path: ".planning/research/phase-1-findings.md"
      provides: "Self-contained Q&A document answering CKPT-05 sub-questions (a) delivery mechanism and (b) context visibility, plus canonical AGENTS.md instruction template"
      contains: "Finding 1, Finding 2, Finding 3, Finding 4, Delivery Mechanism Summary, Trigger Design Summary, Canonical AGENTS.md instruction template"
    - path: ".planning/PROJECT.md"
      provides: "Updated Key Decisions row and Context bullets reflecting Phase 1 outcomes"
      contains: "Context % as handoff trigger | ... | Instructed self-detection (Phase 1)"
    - path: ".planning/ROADMAP.md"
      provides: "Cross-references from Phase 3 and Phase 4 entries to findings doc"
      contains: ".planning/research/phase-1-findings.md"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/research/phase-1-findings.md"
      via: "Key Decisions table outcome column + Context section bullets"
      pattern: "phase-1-findings\\.md"
    - from: ".planning/ROADMAP.md Phase 3 entry"
      to: ".planning/research/phase-1-findings.md"
      via: "Inline reference in phase details"
      pattern: "phase-1-findings\\.md"
    - from: ".planning/ROADMAP.md Phase 4 entry"
      to: ".planning/research/phase-1-findings.md"
      via: "Inline reference in phase details"
      pattern: "phase-1-findings\\.md"
---

<objective>
Finalize Phase 1 research output so that Phase 3 (Checkpoint Schemas + Delivery) and Phase 4 (Handoff Trigger) can proceed with zero re-investigation.

Purpose: The research phase (`/gsd-research-phase`) produced `.planning/research/phase-1-findings.md` containing Q&A on (a) delivery mechanism and (b) context visibility. This plan **audits the findings doc against D-07/D-08 (structured Q&A, self-contained), patches any gaps, and wires the conclusions into PROJECT.md and ROADMAP.md** so downstream phases inherit the decisions automatically. No code is produced — Phase 1 is a research-only spike.

Output: A finalized, self-contained findings doc + PROJECT.md/ROADMAP.md entries that point future implementers at it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-context-api-research/01-CONTEXT.md
@.planning/phases/01-context-api-research/01-RESEARCH.md
@.planning/research/phase-1-findings.md
@AGENTS.md
@src/handlers.ts

<interfaces>
<!-- Key contracts the executor will reference. Already verified by research. -->
<!-- These are FOR REFERENCE during the audit — do not modify src/handlers.ts in this plan. -->

From src/handlers.ts (existing, verified):
```typescript
export interface RunPromptOptions {
  prompt: string;
  system?: string;  // Per-run system override — confirmed effective by Finding 3
  // ... other fields
}

export async function createSession(...);  // body shape: { parentID?: string; title?: string; } — NO system field per Finding 2
export async function runPrompt(opts: RunPromptOptions, ...);  // wires opts.system through to session.prompt body
```

From @opencode-ai/sdk (verified via SDK types):
```typescript
SessionCreateData.body?: { parentID?: string; title?: string; };  // no system field
SessionPromptData.body?: { ...; system?: string; parts: [...]; };  // system IS supported per turn
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and finalize phase-1-findings.md for D-07/D-08 compliance</name>
  <files>.planning/research/phase-1-findings.md</files>
  <read_first>
    - .planning/research/phase-1-findings.md (current state of findings doc)
    - .planning/phases/01-context-api-research/01-CONTEXT.md (D-01..D-08 to verify each is addressed)
    - .planning/phases/01-context-api-research/01-RESEARCH.md (full evidence — source of truth for any patches)
    - .planning/REQUIREMENTS.md (CKPT-05 wording to verify both sub-questions are answered)
    - .planning/ROADMAP.md (Phase 1 Success Criteria 1-4 to verify each is satisfied)
  </read_first>
  <action>
Read the existing `.planning/research/phase-1-findings.md` and audit it against D-07 (structured Q&A with direct answer + evidence per question) and D-08 (self-contained — Phase 3/4 implementers can act on it without reading CONTEXT.md or RESEARCH.md). The doc already contains Findings 1-4, Delivery Mechanism Summary table, Trigger Design Summary table, and one Open Question. Do NOT rewrite it — patch only where the audit identifies a gap.

**Mandatory audit checklist — verify each item is present in the findings doc. If missing, ADD it. Do not remove existing content.**

1. **D-07 structural check:**
   - [ ] Each research question (Q1-Q4) has an explicit "Answer:" line (YES/NO/specific)
   - [ ] Each answer has an "Evidence:" section citing either SDK type path OR live probe transcript OR documentation URL
   - [ ] No question is answered with prose-only handwaving without verifiable evidence

2. **D-08 self-containment check — Phase 3/4 implementer test:** A Phase 3 implementer reading ONLY this doc must learn:
   - [ ] Which file to add checkpoint instructions to (answer: `AGENTS.md` in project root)
   - [ ] Why session-level system prompt is NOT a valid option (answer: silently ignored per Finding 2)
   - [ ] When per-run `system` field is appropriate as a backup (answer: when AGENTS.md absent; see Finding 3)
   - [ ] The exact canonical instruction text to paste into AGENTS.md for checkpoint + Handoff trigger (answer: the code block in the "Trigger Design Summary" section)
   - [ ] That the SDK type for session.create body is `{ parentID?: string; title?: string; }` with no system field

   A Phase 4 implementer reading ONLY this doc must learn:
   - [ ] OpenCode does NOT expose context utilization % to its own agents (Finding 4)
   - [ ] The trigger MUST use instructed self-detection per D-02 (no token counting, no API polling)
   - [ ] The exact instruction-language template to use ("When you sense you are approaching your context limit ...")
   - [ ] That OpenCode's internal compactor runs at 70/80/85/90/99% but is invisible to the agent (Finding 4 architecture note)
   - [ ] The open question about per-run `system` interaction with AGENTS.md (already captured at end of doc — verify it is still there)

3. **Required canonical artifacts in the findings doc — add if missing:**

   a. **Canonical AGENTS.md instruction template** (the verbatim text Phase 3 will paste into `AGENTS.md`). If not already present as a fenced code block, add this section verbatim:

   ```markdown
   ## Canonical AGENTS.md Checkpoint Instruction Template

   Phase 3 implementers: paste the following block into the project `AGENTS.md` under a new `## Checkpointing` section. Do not modify the wording without re-testing the trigger sensitivity.

   ```markdown
   ## Checkpointing

   After each file-modifying tool call (`edit`, `write`, `apply_patch`), update `checkpoint.md` in the working directory with:
   - **current_task:** what you are working on
   - **last_change:** what you just did (file path + one-line summary)
   - **remaining_steps:** what is left
   - **status:** `in_progress` | `complete` | `blocked`

   When you sense you are approaching your context limit — for example, if you have been
   working for a long time, if tracking all state feels difficult, or if the conversation
   feels crowded — write `Handoff.md` in the working directory with:
   - **accomplished:** what was completed this session
   - **current_state:** where the work stands now (which files, which step)
   - **next_steps:** what should happen next, in order
   - **open_questions:** anything you were unsure about

   After writing `Handoff.md`, stop initiating new work in this session. Do not wait for an error.
   ```
   ```

   b. **Phase 3/4 reading checklist** at the top of the doc (under the "Status" line). If not present, add this block verbatim:

   ```markdown
   ## For Phase 3 Implementers (Checkpoint Schemas + Delivery)

   Required reading from this doc:
   - Finding 1: AGENTS.md auto-load mechanism (the delivery vehicle)
   - Finding 2: Why session-level system prompt is unavailable
   - Finding 3: Per-run `system` field as backup
   - Canonical AGENTS.md Checkpoint Instruction Template (paste this verbatim)
   - Delivery Mechanism Summary table

   You DO NOT need to read: `.planning/phases/01-context-api-research/01-CONTEXT.md`, `01-RESEARCH.md`, or rerun any OpenCode API probes.

   ## For Phase 4 Implementers (Handoff Trigger)

   Required reading from this doc:
   - Finding 4: Context utilization is NOT visible to the agent
   - Trigger Design Summary table
   - Canonical AGENTS.md Checkpoint Instruction Template (Handoff paragraph)
   - Open Question: per-run `system` vs AGENTS.md interaction (test this if you need both)

   You DO NOT need to read: `.planning/phases/01-context-api-research/01-CONTEXT.md` or `01-RESEARCH.md`.
   ```

   c. **Version stamps** — confirm the top of the doc states `OpenCode version tested: 1.14.48` and `SDK version: @opencode-ai/sdk 1.14.25` (already present per the current content — verify they remain).

4. **Apply minimal patches:** Use the Edit tool to add only the missing pieces. Preserve all existing content (Findings 1-4, Delivery Mechanism Summary, Trigger Design Summary, Open Question, Source footer). Do NOT reorder existing sections. Do NOT delete anything.

5. **Verify after patching:**
   - `grep -n "## Canonical AGENTS.md Checkpoint Instruction Template" .planning/research/phase-1-findings.md` returns exactly one match
   - `grep -n "## For Phase 3 Implementers" .planning/research/phase-1-findings.md` returns exactly one match
   - `grep -n "## For Phase 4 Implementers" .planning/research/phase-1-findings.md` returns exactly one match
   - `grep -c "## Finding " .planning/research/phase-1-findings.md` returns 4 (Findings 1-4 still present)

If ALL audit checklist items above were ALREADY present in the pre-existing doc, this task may be a no-op for content — but you MUST still leave a short audit note as the final line of the doc:
```
*Audit: 2026-05-11 — verified D-07 (structured Q&A) and D-08 (self-contained for Phase 3/4) compliance.*
```
  </action>
  <verify>
    <automated>test -f .planning/research/phase-1-findings.md && grep -q "## Canonical AGENTS.md Checkpoint Instruction Template" .planning/research/phase-1-findings.md && grep -q "## For Phase 3 Implementers" .planning/research/phase-1-findings.md && grep -q "## For Phase 4 Implementers" .planning/research/phase-1-findings.md && [ "$(grep -c '^## Finding ' .planning/research/phase-1-findings.md)" -ge 4 ] && grep -q "Audit:" .planning/research/phase-1-findings.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/research/phase-1-findings.md` exists
    - File contains exact heading `## Canonical AGENTS.md Checkpoint Instruction Template`
    - File contains exact heading `## For Phase 3 Implementers (Checkpoint Schemas + Delivery)`
    - File contains exact heading `## For Phase 4 Implementers (Handoff Trigger)`
    - File contains at least 4 lines starting with `## Finding ` (Findings 1-4 preserved)
    - File contains the literal string `OpenCode version tested: 1.14.48`
    - File contains the literal string `@opencode-ai/sdk 1.14.25`
    - File contains the literal string `Audit:` on the audit-note line at the end
    - File contains the literal string `checkpoint.md` (canonical filename for the per-edit checkpoint)
    - File contains the literal string `Handoff.md` (canonical filename for the context-pressure handoff)
    - File contains the literal phrase `instructed self-detection` (matches D-02 vocabulary)
    - File contains the literal phrase `silently ignored` (Finding 2 wording about session-level system prompt)
  </acceptance_criteria>
  <done>Findings doc audited; canonical AGENTS.md instruction template + Phase 3/4 reader checklists added (or confirmed present); audit note appended.</done>
</task>

<task type="auto">
  <name>Task 2: Update PROJECT.md Key Decisions and Context to reflect Phase 1 outcomes</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (current file — Key Decisions table row "Context % as handoff trigger" is "Pending"; Context bullets say delivery mechanism + context API are "unknown")
    - .planning/research/phase-1-findings.md (source of truth — finalized in Task 1)
    - .planning/phases/01-context-api-research/01-CONTEXT.md (D-02 wording for the new decision outcome)
  </read_first>
  <action>
Update `.planning/PROJECT.md` to retire all "Pending Phase 1 research" language with the actual Phase 1 outcomes. The findings doc is now the authoritative source — PROJECT.md must point at it and stop saying "unknown".

**Exact edits:**

1. **In the "Active" requirements list (lines ~33-36), replace the two "TBD pending Phase 1 research" parenthetical clauses:**

   Replace this line:
   ```
   - [ ] Prefect agents receive checkpoint instructions and write checkpoint.md after each file-modifying tool call (delivery mechanism TBD pending Phase 1 research)
   ```
   with:
   ```
   - [ ] Prefect agents receive checkpoint instructions and write checkpoint.md after each file-modifying tool call (delivery mechanism: AGENTS.md auto-load — see `.planning/research/phase-1-findings.md` Finding 1)
   ```

   Replace this line:
   ```
   - [ ] Handoff.md written by prefect agent when its context reaches ~80% (trigger mechanism TBD pending Phase 1 research)
   ```
   with:
   ```
   - [ ] Handoff.md written by prefect agent when context pressure is sensed (trigger mechanism: instructed self-detection — OpenCode does not expose context % to agents; see `.planning/research/phase-1-findings.md` Finding 4)
   ```

2. **In the "Context" section, replace the two "unknown" bullets:**

   Replace this bullet:
   ```
   - Checkpoint instructions must be delivered to prefect agents (OpenCode agents), not to Claude Code — the delivery mechanism (session system prompt, per-run pre-prompt, OpenCode Agent.md, etc.) is unknown and is the subject of Phase 1 research
   ```
   with:
   ```
   - Checkpoint instructions are delivered to prefect agents via AGENTS.md auto-load in the working directory (Phase 1 research finding). Per-run `system` field in `prefect_run` is a confirmed backup mechanism. Session-level system prompt at creation is NOT supported. See `.planning/research/phase-1-findings.md`.
   ```

   Replace this bullet:
   ```
   - OpenCode context utilization API: unknown — needs research before implementing the 80% cutoff trigger for prefect agents
   ```
   with:
   ```
   - OpenCode does NOT expose context utilization % to its own agents (Phase 1 research finding). The Handoff.md trigger therefore uses instructed self-detection — the agent's own judgment on when context feels crowded — not a token-count or percentage threshold. See `.planning/research/phase-1-findings.md` Finding 4.
   ```

3. **In the "Key Decisions" table, update the "Context % as handoff trigger" row's Outcome column:**

   Replace this row:
   ```
   | Context % as handoff trigger (not message count) | Tied to actual resource pressure rather than arbitrary message count | — Pending (needs OpenCode research) |
   ```
   with:
   ```
   | Handoff trigger via instructed self-detection (not context %) | OpenCode does not surface context % to agents; LLM self-judgment is the only available signal — see `.planning/research/phase-1-findings.md` Finding 4 | ✓ Decided (Phase 1) |
   ```

4. **Do NOT change any other section** (What This Is, Core Value, Current Milestone, Validated requirements, Out of Scope, Evolution).

5. **Verify after editing:**
   - `grep -n "Pending Phase 1\|pending Phase 1\|TBD pending Phase 1\|unknown — needs research\|unknown and is the subject" .planning/PROJECT.md` returns zero matches
   - `grep -c "phase-1-findings.md" .planning/PROJECT.md` returns at least 4 (one per edited bullet/row)
  </action>
  <verify>
    <automated>! grep -q "TBD pending Phase 1 research" .planning/PROJECT.md && ! grep -q "unknown and is the subject of Phase 1 research" .planning/PROJECT.md && ! grep -q "unknown — needs research" .planning/PROJECT.md && ! grep -q "— Pending (needs OpenCode research)" .planning/PROJECT.md && [ "$(grep -c "phase-1-findings.md" .planning/PROJECT.md)" -ge 4 ] && grep -q "instructed self-detection" .planning/PROJECT.md && grep -q "AGENTS.md auto-load" .planning/PROJECT.md && grep -q "✓ Decided (Phase 1)" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains zero occurrences of `TBD pending Phase 1 research`
    - `.planning/PROJECT.md` contains zero occurrences of `unknown and is the subject of Phase 1 research`
    - `.planning/PROJECT.md` contains zero occurrences of `unknown — needs research`
    - `.planning/PROJECT.md` contains zero occurrences of `— Pending (needs OpenCode research)`
    - `.planning/PROJECT.md` contains at least 4 occurrences of `phase-1-findings.md`
    - `.planning/PROJECT.md` contains the literal phrase `instructed self-detection`
    - `.planning/PROJECT.md` contains the literal phrase `AGENTS.md auto-load`
    - `.planning/PROJECT.md` Key Decisions table contains a row with `✓ Decided (Phase 1)` in the Outcome column
    - `.planning/PROJECT.md` Active requirements list still contains exactly two `[ ]` checkpoint/handoff bullets (one for checkpoint.md, one for Handoff.md) — count preserved
  </acceptance_criteria>
  <done>PROJECT.md no longer says any Phase 1 research question is "Pending" or "unknown"; all four affected bullets/rows cite `phase-1-findings.md`; the Handoff trigger decision is logged as "✓ Decided (Phase 1)".</done>
</task>

<task type="auto">
  <name>Task 3: Add findings cross-references to ROADMAP.md Phase 3 and Phase 4 entries</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (current file — Phase 3 Goal says "wired via the delivery mechanism found in Phase 1"; Phase 4 Goal says "using the trigger mechanism identified in Phase 1" — neither links to the findings doc)
    - .planning/research/phase-1-findings.md (the doc to link to — finalized in Task 1)
  </read_first>
  <action>
Update `.planning/ROADMAP.md` so that Phase 3 and Phase 4 entries point future planners directly at the findings doc, satisfying ROADMAP Phase 1 Success Criterion 4 ("Findings are recorded in `.planning/research/` so Phase 3 and Phase 4 implementers need no re-investigation").

**Exact edits:**

1. **Append a "Findings:" line to the Phase 3 details block.** Locate the Phase 3 section (header `### Phase 3: Checkpoint Schemas + Delivery`) and add a new line directly AFTER the `**Depends on**: Phase 1 (delivery mechanism)` line and BEFORE the `**Requirements**:` line:

   ```
   **Findings (Phase 1 → Phase 3 inputs)**: `.planning/research/phase-1-findings.md` — Finding 1 (AGENTS.md auto-load), Finding 2 (no session-level system prompt), Finding 3 (per-run `system` backup), Canonical AGENTS.md Checkpoint Instruction Template
   ```

2. **Append a "Findings:" line to the Phase 4 details block.** Locate the Phase 4 section (header `### Phase 4: Handoff Trigger`) and add a new line directly AFTER the `**Depends on**: Phase 1 (trigger design), Phase 3 (Handoff.md schema and delivery wiring)` line and BEFORE the `**Requirements**:` line:

   ```
   **Findings (Phase 1 → Phase 4 inputs)**: `.planning/research/phase-1-findings.md` — Finding 4 (context % NOT visible to agent), Trigger Design Summary (instructed self-detection per D-02), Open Question (per-run `system` vs AGENTS.md interaction — test at implementation time)
   ```

3. **Append a "Findings:" line to the Phase 1 details block** so its own success criterion 4 is satisfied in the same document. Locate the Phase 1 section (header `### Phase 1: Context API Research`) and add a new line directly AFTER the `**Requirements**: CKPT-05` line and BEFORE the `**Success Criteria** (what must be TRUE):` line:

   ```
   **Findings**: `.planning/research/phase-1-findings.md` (self-contained Q&A; Phase 3/4 read this — not CONTEXT.md or RESEARCH.md)
   ```

4. **Do NOT change the Overview, Phases checklist (lines 8-18), Progress table, or any other phase's content.** Do NOT change Phase 2. Do NOT renumber phases.

5. **Verify after editing:**
   - `grep -c "phase-1-findings.md" .planning/ROADMAP.md` returns exactly 3 (one per phase block: 1, 3, 4)
   - The three new lines appear under the correct phase headers (verified by line-order grep)
  </action>
  <verify>
    <automated>[ "$(grep -c "phase-1-findings.md" .planning/ROADMAP.md)" -eq 3 ] && grep -q "Findings (Phase 1 → Phase 3 inputs)" .planning/ROADMAP.md && grep -q "Findings (Phase 1 → Phase 4 inputs)" .planning/ROADMAP.md && awk '/^### Phase 1:/,/^### Phase 2:/' .planning/ROADMAP.md | grep -q "phase-1-findings.md" && awk '/^### Phase 3:/,/^### Phase 4:/' .planning/ROADMAP.md | grep -q "phase-1-findings.md" && awk '/^### Phase 4:/,/^## Progress/' .planning/ROADMAP.md | grep -q "phase-1-findings.md"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains exactly 3 occurrences of `phase-1-findings.md`
    - `.planning/ROADMAP.md` contains the literal string `Findings (Phase 1 → Phase 3 inputs)`
    - `.planning/ROADMAP.md` contains the literal string `Findings (Phase 1 → Phase 4 inputs)`
    - The Phase 1 block (between `### Phase 1:` and `### Phase 2:`) contains `phase-1-findings.md` (verified with awk range grep)
    - The Phase 3 block (between `### Phase 3:` and `### Phase 4:`) contains `phase-1-findings.md` (verified with awk range grep)
    - The Phase 4 block (between `### Phase 4:` and `## Progress`) contains `phase-1-findings.md` (verified with awk range grep)
    - The Phases bullet list (lines starting with `- [ ] **Phase`) still contains exactly 4 entries (Phase 1, 2, 3, 4) — no phases added or removed
    - The Progress table at the bottom is unchanged (still lists 4 phases with their existing status)
  </acceptance_criteria>
  <done>ROADMAP.md Phase 1, 3, and 4 entries each cite `phase-1-findings.md` with a one-line summary of which findings apply; no other content changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Research doc → downstream phases | Phase 3/4 implementers will paste instruction templates from this doc into AGENTS.md and write code against the claimed SDK shapes. Incorrect or stale claims propagate into runtime behavior. |
| OpenCode API surface → findings doc | The findings doc claims specific SDK type shapes and API behaviors. If OpenCode releases a major version, those claims may become stale. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `.planning/research/phase-1-findings.md` content | mitigate | Each finding includes explicit "Answer:" + "Evidence:" sections with SDK type paths or live probe transcripts; audit task enforces this structure (D-07). Acceptance criteria require exact strings like `silently ignored` (Finding 2) and `instructed self-detection` (D-02 vocabulary) to be present, preventing silent rewording that drifts from research. |
| T-01-02 | Information disclosure | `.planning/research/phase-1-findings.md` versioning | accept | Findings doc top contains `OpenCode version tested: 1.14.48` and `@opencode-ai/sdk 1.14.25` and the original RESEARCH.md states `Valid until: 2026-08-11`. If OpenCode upgrades, Phase 3/4 implementers will see the version stamps and can re-verify. Risk is low (research is current; downstream phases will execute soon). |
| T-01-03 | Repudiation | PROJECT.md "Pending" → "Decided" transitions | mitigate | Each transitioning row/bullet in PROJECT.md and ROADMAP.md cites the specific finding in `phase-1-findings.md` by filename and finding number. Audit trail is grep-able. |
| T-01-04 | Denial of service (planner self-DOS) | Phase 3/4 re-investigation | mitigate | Findings doc includes explicit "For Phase 3 Implementers" and "For Phase 4 Implementers" reader checklists with `You DO NOT need to read: ...` clauses. ROADMAP.md Phase 3/4 entries link directly to the findings doc. Together these eliminate re-investigation. |

No high-severity threats. ASVS L1: no input handling, no auth, no user data, no executable code surface introduced by this phase.
</threat_model>

<verification>
After all three tasks complete, run these checks from the project root:

```bash
# Task 1 verification — findings doc is self-contained for Phase 3/4
grep -q "## Canonical AGENTS.md Checkpoint Instruction Template" .planning/research/phase-1-findings.md
grep -q "## For Phase 3 Implementers" .planning/research/phase-1-findings.md
grep -q "## For Phase 4 Implementers" .planning/research/phase-1-findings.md
grep -q "instructed self-detection" .planning/research/phase-1-findings.md

# Task 2 verification — PROJECT.md retired all Phase 1 placeholders
! grep -q "TBD pending Phase 1 research" .planning/PROJECT.md
! grep -q "— Pending (needs OpenCode research)" .planning/PROJECT.md
[ "$(grep -c "phase-1-findings.md" .planning/PROJECT.md)" -ge 4 ]

# Task 3 verification — ROADMAP.md cross-references in place
[ "$(grep -c "phase-1-findings.md" .planning/ROADMAP.md)" -eq 3 ]

# Cross-phase reachability — Phase 3/4 implementers landing on ROADMAP can find the findings doc
awk '/^### Phase 3:/,/^### Phase 4:/' .planning/ROADMAP.md | grep -q "phase-1-findings.md"
awk '/^### Phase 4:/,/^## Progress/' .planning/ROADMAP.md | grep -q "phase-1-findings.md"
```

All commands MUST exit 0.
</verification>

<success_criteria>
- [ ] `.planning/research/phase-1-findings.md` contains canonical AGENTS.md instruction template, Phase 3 reader checklist, Phase 4 reader checklist, and audit note
- [ ] `.planning/PROJECT.md` no longer says any Phase 1 question is "Pending" / "unknown" / "TBD"; all four affected bullets/rows cite `phase-1-findings.md`
- [ ] `.planning/ROADMAP.md` Phase 1, Phase 3, and Phase 4 blocks each contain a `phase-1-findings.md` reference line
- [ ] ROADMAP Phase 1 Success Criteria 1-4 are all satisfied (findings answer both sub-questions, specify exact mechanisms, are recorded in `.planning/research/`)
- [ ] Requirement CKPT-05 is closeable: both sub-questions (delivery mechanism, context visibility) have direct answers with evidence in the findings doc
- [ ] No source code (`src/**`) was modified — this is a research-only phase
</success_criteria>

<output>
After completion, create `.planning/phases/01-context-api-research/01-01-SUMMARY.md` documenting:
- What was finalized in the findings doc (which audit gaps were patched, if any)
- The exact PROJECT.md rows/bullets that transitioned from "Pending/unknown" to "Decided"
- The three ROADMAP.md insertion points
- A one-line statement confirming Phase 3 and Phase 4 implementers can act on the findings doc alone
</output>
