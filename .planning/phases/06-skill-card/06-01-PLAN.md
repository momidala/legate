---
phase: 06-skill-card
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/cli.test.ts
autonomous: true
requirements:
  - SKILL-01
  - SKILL-03
  - SKILL-04
  - SKILL-05
tags:
  - tdd
  - cli
  - skill-card
  - wave-0

must_haves:
  truths:
    - "Test file contains a RED assertion proving legate.md is written to ~/.claude/commands/ when legate init runs"
    - "Test file contains a RED assertion proving legate-update.md is written to ~/.claude/commands/ when legate init runs"
    - "Test file contains a RED assertion proving legate.md content includes the canonical loop heading and at least one legate_* tool name"
    - "Test file contains a RED assertion proving legate.md workers section reflects servers.json contents (named server) and the empty-registry placeholder when no servers exist"
    - "Test file contains a RED assertion proving second legate init overwrites legate.md and legate-update.md (idempotent)"
    - "Test file contains a RED assertion proving legate uninstall-command removes BOTH legate.md and legate-update.md"
    - "Existing SELFUP warning-message assertion is updated to expect 'Warning: legate commands not installed —' (currently asserts 'Warning: legate-update command not installed —')"
    - "All new tests currently FAIL because src/cli.ts implementation does not yet exist (Plan 02 turns them GREEN)"
  artifacts:
    - path: "src/cli.test.ts"
      provides: "SKILL-01..SKILL-05 integration tests appended to existing SELFUP block"
      contains: "test('SKILL-01"
  key_links:
    - from: "src/cli.test.ts"
      to: "build/cli.js"
      via: "runCliAsGlobal helper + spawnSync"
      pattern: "runCliAsGlobal\\(.*'init'\\)"
    - from: "src/cli.test.ts"
      to: "~/.claude/commands/legate.md (tmp HOME)"
      via: "existsSync + readFileSync assertion"
      pattern: "join\\(.*'.claude', 'commands', 'legate.md'\\)"
---

<objective>
Add failing integration tests in `src/cli.test.ts` for SKILL-01, SKILL-03, SKILL-04, and SKILL-05. Update the existing SELFUP warning-message assertion (line 568) to match the new wording Plan 02 will emit. Every new test MUST fail when run today because the production code under test (Plan 02) does not yet exist; this satisfies the Nyquist Wave 0 contract recorded in 06-VALIDATION.md.

Purpose: Lock the SKILL behavioural contracts as executable RED tests before any implementation lands. Plan 02 must turn these GREEN.

Output: An updated `src/cli.test.ts` containing new SKILL-01..SKILL-05 tests appended after line 585, plus a one-line edit on the existing warning-message assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-skill-card/06-RESEARCH.md
@.planning/phases/06-skill-card/06-PATTERNS.md
@.planning/phases/06-skill-card/06-VALIDATION.md
@src/cli.test.ts
@src/cli.ts
@src/registry.ts

<interfaces>
<!-- Test helpers already present in src/cli.test.ts (lines 1-26 and 443-467). Reuse them; do not redefine. -->

Imports at top of src/cli.test.ts (already in place):
```
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { spawnSync } from 'node:child_process';
import { mkdtempSync, mkdirSync, writeFileSync, readFileSync, existsSync, rmSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { resolve, join } from 'node:path';
```

Helpers (already defined — reuse, do not redeclare):
- freshTmp(): string                                            // line 13
- runCli(cwd, env, ...args): { status, stdout, stderr }         // line 22
- runCliAsGlobal(homeDir, ...args): { status, stdout, stderr }  // line 446
  // Sets HOME=homeDir, USERPROFILE=homeDir, npm_config_global='true'.
  // Copies build/cli.js + build/registry.js into <homeDir>/node_modules/legate/build/.

Registry shape (from src/registry.ts):
```
type ServerEntry = {
  name: string;
  host: string;
  port: number;
  providerID: string;
  modelID: string;
  maxSessions?: number;
};
type Registry = { servers: ServerEntry[] };
```
readRegistry() reads ~/.config/legate/servers.json (override HOME for test isolation).
Empty/ENOENT returns { servers: [] } — does not throw.

CLI subcommands invoked in these tests:
- `node build/cli.js init`                  (NOT global — but Plan 02 makes init unconditionally write skill cards)
- `node build/cli.js install-command`       (global path — npm postinstall analogue; also writes both files after Plan 02)
- `node build/cli.js uninstall-command`     (global path — removes both files after Plan 02)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append SKILL-01, SKILL-04, SKILL-05 RED tests for legate init writing both skill card files (and overwriting on reinstall)</name>
  <files>src/cli.test.ts</files>
  <read_first>
    - src/cli.test.ts (full file — helpers at lines 1-26 and 443-467; SELFUP analog tests at lines 469-585)
    - src/cli.ts (current init case at lines 205-248; install/uninstall handlers at lines 147-181)
    - .planning/phases/06-skill-card/06-RESEARCH.md (Pattern 4 init integration, Pitfall 1 isGlobal guard, Pitfall 3 empty registry)
    - .planning/phases/06-skill-card/06-PATTERNS.md (Test structure pattern lines 162-184; runCliAsGlobal usage; freshTmp + HOME override)
    - .planning/phases/06-skill-card/06-VALIDATION.md (Wave 0 row mapping: 06-01-01 -> SKILL-01, 06-01-02 -> SKILL-04, 06-01-04 -> SKILL-05)
  </read_first>
  <behavior>
    - Test SKILL-01a "legate init writes ~/.claude/commands/legate.md (non-global path)": `runCli(dir, { ...process.env, HOME: dir, USERPROFILE: dir }, 'init')` MUST exit 0, AND `existsSync(join(dir, '.claude', 'commands', 'legate.md'))` MUST be true. Currently RED because production code in src/cli.ts init case never writes legate.md.
    - Test SKILL-04a "legate init writes ~/.claude/commands/legate-update.md (non-global path)": same setup as SKILL-01a; assert `existsSync(join(dir, '.claude', 'commands', 'legate-update.md'))` is true. Currently RED because init does not call installSkillCards yet.
    - Test SKILL-05a "second legate init overwrites both files (idempotent)": run init twice; between runs, mutate the existing `legate.md` content (write the string `STALE_MARKER` to it) and read it back after the second init — content MUST NOT contain `STALE_MARKER`. Currently RED for same reason.
    - All three tests use the existing `freshTmp()` / `runCli(dir, env, 'init')` pattern with `HOME=dir, USERPROFILE=dir` overrides and a `try { ... } finally { rmSync(dir, { recursive: true, force: true }); }` cleanup wrapper (matches existing SELFUP tests at lines 469-481).
    - Verify the tests are RED before completing the task: run `npm run build && node --test build/cli.test.js 2>&1 | grep -E '^# (fail|tests)'` and confirm at least 3 failures attributable to the new SKILL-01a / SKILL-04a / SKILL-05a names.
  </behavior>
  <action>
    Append three new `test(...)` blocks to the END of src/cli.test.ts (after the closing brace of the current final test at approx line 585; do not modify any existing test). Name them exactly: `'SKILL-01: legate init writes ~/.claude/commands/legate.md'`, `'SKILL-04: legate init writes ~/.claude/commands/legate-update.md'`, and `'SKILL-05: second legate init overwrites both skill card files'`. Each test follows the SELFUP-01 structural template at cli.test.ts lines 496-513: freshTmp() -> try block -> `runCli(dir, { ...process.env, HOME: dir, USERPROFILE: dir }, 'init')` -> assert `status === 0` -> assert existsSync (and for SKILL-05, write `STALE_MARKER` between two init calls then assert the post-second-init file does not contain `STALE_MARKER`) -> finally rmSync. Do not import any new helpers; everything required is already imported at the top of the file. After writing, run the build+test command above and confirm new tests fail with assertion errors (not syntax errors); commit only after RED is observed.
  </action>
  <verify>
    <automated>npm run build && node --test build/cli.test.js 2>&1 | grep -E "SKILL-01: legate init writes|SKILL-04: legate init writes|SKILL-05: second legate init overwrites" | grep -E "not ok|✖|fail"</automated>
  </verify>
  <acceptance_criteria>
    - src/cli.test.ts contains the literal string `test('SKILL-01: legate init writes ~/.claude/commands/legate.md'`
    - src/cli.test.ts contains the literal string `test('SKILL-04: legate init writes ~/.claude/commands/legate-update.md'`
    - src/cli.test.ts contains the literal string `test('SKILL-05: second legate init overwrites both skill card files'`
    - Each new test body calls `runCli(dir,` with `HOME: dir` and `USERPROFILE: dir` in its env override (grep finds at least 3 occurrences inside the new SKILL block)
    - Each new test body contains a `try { ... } finally { rmSync(dir, { recursive: true, force: true }); }` cleanup
    - `npm run build && node --test build/cli.test.js` exits non-zero (RED) AND its output names the three new SKILL tests as failing
    - No existing SELFUP test is removed or renamed (grep count of `test('SELFUP` remains identical to pre-task count)
  </acceptance_criteria>
  <done>Three SKILL-01a / SKILL-04a / SKILL-05a tests committed and verified RED. Existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append SKILL-03 RED tests for skill card content (canonical loop, tool table, workers section with and without registry entries)</name>
  <files>src/cli.test.ts</files>
  <read_first>
    - src/cli.test.ts (top imports lines 1-6; freshTmp lines 13-15; runCli lines 22-26; add-server test at lines 254-265 for the registry-write pattern)
    - src/cli.ts (existing LEGATE_UPDATE_COMMAND_CONTENT lines 35-44 — the pattern Plan 02 mirrors; existing updateClaudemdWorkers bullet format lines 51-55)
    - src/registry.ts (full file — readRegistry signature and ENOENT-as-empty behaviour)
    - .planning/phases/06-skill-card/06-RESEARCH.md (Pattern 1 LEGATE_SKILL_CARD_STATIC; Pitfall 2 tool list verification; Pitfall 3 empty registry; Code Examples section — actual servers.json format and exact bullet pattern)
    - .planning/phases/06-skill-card/06-PATTERNS.md (Change 1 constant pattern; Change 2 buildWorkersSection rule)
    - .planning/phases/06-skill-card/06-VALIDATION.md (Wave 0 row 06-01-03 -> SKILL-03)
  </read_first>
  <behavior>
    - Test SKILL-03a "legate.md contains canonical loop and tool table headings": after `runCli(dir, env, 'init')` with HOME=dir, read `<dir>/.claude/commands/legate.md` and assert content matches all of:
        - `/# Legate — Skill Card/`
        - `/## Canonical Loop/`
        - `/legate_create_session/`
        - `/legate_run/`
        - `/legate_session_delete/`
        - `/## Tools/` (or a tool-group table header — keep regex generic: `/Tools \(40 total/` will fail if Plan 02 chooses a different phrasing, so use `/## Tools/` then assert at least 6 `legate_` references appear in the file via a follow-up regex count: `(content.match(/legate_[a-z_]+/g) ?? []).length >= 6`)
    - Test SKILL-03b "legate.md workers section reflects servers.json when registered": before `init`, run `runCli(dir, env, 'add-server', 'thor', 'localhost', '4096', 'vllm', 'qwen3-coder')` to populate `<dir>/.config/legate/servers.json`. Then run `runCli(dir, env, 'init')`. Read legate.md; assert content matches `/## Available Workers/` AND `/\*\*thor\*\* — vllm\/qwen3-coder, localhost:4096/` (the exact bullet format from updateClaudemdWorkers at src/cli.ts:51-55).
    - Test SKILL-03c "legate.md workers section shows empty-registry placeholder": run `init` in a fresh tmp dir with NO add-server calls (so no servers.json exists). Read legate.md; assert `/## Available Workers/` AND `/no servers registered/`.
    - All three tests RED today: Plan 02 has not yet defined `LEGATE_SKILL_CARD_STATIC` or `buildWorkersSection`, and the init case does not write legate.md.
  </behavior>
  <action>
    Append three more `test(...)` blocks to src/cli.test.ts immediately after the Task 1 tests, named exactly: `'SKILL-03: legate.md contains canonical loop and tool table'`, `'SKILL-03: legate.md workers section reflects registered servers'`, and `'SKILL-03: legate.md workers section shows placeholder when registry empty'`. Use the existing `freshTmp` + `runCli(dir, env, ...)` helpers with `HOME=dir, USERPROFILE=dir` env override. For the SKILL-03b registered-server test, invoke `runCli(dir, env, 'add-server', 'thor', 'localhost', '4096', 'vllm', 'qwen3-coder')` before invoking `runCli(dir, env, 'init')`. Read the produced legate.md via `readFileSync(join(dir, '.claude', 'commands', 'legate.md'), 'utf8')` and use `assert.match(content, /regex/)` per behavior block above. For the "at least 6 legate_ tool references" check, use `assert.ok((content.match(/legate_[a-z_]+/g) ?? []).length >= 6, 'expected at least 6 legate_ tool refs')`. Wrap each test body in try/finally rmSync cleanup. After writing, run the build+test command and confirm RED.
  </action>
  <verify>
    <automated>npm run build && node --test build/cli.test.js 2>&1 | grep -E "SKILL-03: legate.md contains|SKILL-03: legate.md workers section reflects|SKILL-03: legate.md workers section shows placeholder" | grep -E "not ok|✖|fail"</automated>
  </verify>
  <acceptance_criteria>
    - src/cli.test.ts contains the literal strings `test('SKILL-03: legate.md contains canonical loop and tool table'`, `test('SKILL-03: legate.md workers section reflects registered servers'`, and `test('SKILL-03: legate.md workers section shows placeholder when registry empty'`
    - SKILL-03b test invokes `runCli(dir, env, 'add-server', 'thor', 'localhost', '4096', 'vllm', 'qwen3-coder')` (literal string match)
    - SKILL-03b test asserts the literal pattern `/\*\*thor\*\* — vllm\/qwen3-coder, localhost:4096/` (or equivalent assert.match call against the same bullet format)
    - SKILL-03a contains an assert.match against `/## Canonical Loop/` and an assertion proving at least 6 distinct `legate_*` tool name references appear in legate.md
    - `npm run build && node --test build/cli.test.js` exits non-zero AND output names all three SKILL-03 tests as failing
    - No previously-passing test now fails for any reason other than the deliberately introduced new SKILL tests (compare count of failing tests before vs after — delta must equal new test count only)
  </acceptance_criteria>
  <done>Three SKILL-03 content tests committed and confirmed RED via `node --test build/cli.test.js`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Append SKILL-05 uninstall-removes-both test AND update existing warning-message assertion at line 568</name>
  <files>src/cli.test.ts</files>
  <read_first>
    - src/cli.test.ts (existing SELFUP-02 uninstall test at lines 529-557; existing warning-message test at lines 559-572 — the assertion on line 568 MUST be edited)
    - src/cli.ts (handleUninstallCommand at lines 169-181; handleInstallCommand at lines 147-167 — note the current warning string `Warning: legate-update command not installed —`)
    - .planning/phases/06-skill-card/06-RESEARCH.md (Pitfall 4 uninstall must remove legate.md; Pitfall 6 warning message changes)
    - .planning/phases/06-skill-card/06-PATTERNS.md (Change 4 handleUninstallCommand rule; Exact warning message assertion pattern at lines 211-218)
    - .planning/phases/06-skill-card/06-VALIDATION.md (Wave 0 row 06-01-04 -> SKILL-05)
  </read_first>
  <behavior>
    - Test SKILL-05b "uninstall-command removes both legate.md and legate-update.md": invoke `runCliAsGlobal(dir, 'install-command')` to install both files (relies on Plan 02 making install-command also write legate.md), then `runCliAsGlobal(dir, 'uninstall-command')`. Assert exit status 0, AND `existsSync(<dir>/.claude/commands/legate.md) === false`, AND `existsSync(<dir>/.claude/commands/legate-update.md) === false`. Currently RED: pre-Plan-02 install-command does not create legate.md, so the "precondition file must exist after install" inner assertion fails before the uninstall assertions even run.
    - Update the existing test `'SELFUP: install-command warns to stderr and exits 0 when mkdir/write fails'` (currently at lines 559-572 of cli.test.ts): the assertion `assert.match(stderr, /Warning: legate-update command not installed —/)` on line 568 must be changed to `assert.match(stderr, /Warning: legate commands not installed —/)` to match the new combined-files wording Plan 02 will emit. After this edit, that test goes RED until Plan 02 lands. Do not delete or rename the test; only change the regex on the one assertion line.
    - Also update the `'SELFUP: legate bogus usage lists install-command and uninstall-command'` test (currently at lines 574-585): the assertions at lines 580-581 currently match `/install-command\s+Install \/legate-update Claude command/` and `/uninstall-command\s+Remove \/legate-update Claude command/`. Plan 02 will broaden the usage strings to mention both files. Change those two regexes to `/install-command\s+Install \/legate and \/legate-update Claude commands/` and `/uninstall-command\s+Remove \/legate and \/legate-update Claude commands/`.
  </behavior>
  <action>
    1) Append a new `test('SKILL-05: uninstall-command removes both legate.md and legate-update.md', () => { ... })` block immediately after the Task 2 tests. Inside, use `runCliAsGlobal(dir, 'install-command')` then `runCliAsGlobal(dir, 'uninstall-command')`. Assert: install run exits 0, both files exist after install (precondition), uninstall exits 0, uninstall stderr === '', and existsSync for BOTH file paths is false after uninstall. Wrap in try/finally rmSync cleanup matching SELFUP-02 at lines 529-544.
    2) Edit the EXISTING test at approximately line 559 (`SELFUP: install-command warns to stderr and exits 0 when mkdir/write fails`): change the regex on the `assert.match(stderr, ...)` line from `/Warning: legate-update command not installed —/` to `/Warning: legate commands not installed —/`. Do not change any other line in that test.
    3) Edit the EXISTING test at approximately line 574 (`SELFUP: legate bogus usage lists install-command and uninstall-command`): change the two regexes per the behavior block above (install-command and uninstall-command usage strings).
    4) Run `npm run build && node --test build/cli.test.js`; confirm: new SKILL-05b is RED, the two edited SELFUP tests are now RED (because Plan 02 has not yet emitted the new strings), and no other test changed status.
  </action>
  <verify>
    <automated>npm run build && node --test build/cli.test.js 2>&1 | grep -cE "SKILL-05: uninstall-command removes both|Warning: legate commands not installed|install-command\\\\s\\+Install \\\\/legate and"</automated>
  </verify>
  <acceptance_criteria>
    - src/cli.test.ts contains the literal string `test('SKILL-05: uninstall-command removes both legate.md and legate-update.md'`
    - `grep -c "Warning: legate-update command not installed" src/cli.test.ts` returns 0 (the old assertion is fully replaced)
    - `grep -c "Warning: legate commands not installed" src/cli.test.ts` returns at least 1
    - `grep -c "Install /legate-update Claude command" src/cli.test.ts` returns 0
    - `grep -c "Install /legate and /legate-update Claude commands" src/cli.test.ts` returns at least 1
    - `grep -c "Remove /legate and /legate-update Claude commands" src/cli.test.ts` returns at least 1
    - `npm run build && node --test build/cli.test.js` exits non-zero AND output shows: SKILL-05 uninstall test failing, the updated SELFUP warning test failing, and the updated SELFUP bogus-usage test failing
    - No SELFUP test is deleted or renamed (count of `test('SELFUP` lines remains identical to pre-task count)
  </acceptance_criteria>
  <done>SKILL-05b test added; warning-message and bogus-usage assertions retargeted to new wording. Test suite RED. Plan 02 will turn all of these GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local fs ↔ test temp dir | tests write to mkdtempSync paths under tmpdir() and set HOME/USERPROFILE to redirect ~/.claude and ~/.config reads/writes into those paths — no real user state touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | tmp HOME isolation in tests | mitigate | every new test wraps body in `try { ... } finally { rmSync(dir, { recursive: true, force: true }); }` (matches SELFUP pattern at cli.test.ts:478-480) so a failing assertion never leaks tmp directories; HOME/USERPROFILE override ensures no write touches the real `~/.claude/commands/` or `~/.config/legate/` |
| T-06-02 | Denial of Service | spawnSync runaway in tests | accept | per existing test infra — node:test default timeout governs; no new spawn unbounded loops introduced |
| T-06-03 | Information Disclosure | accidental write to real `~/.claude` | mitigate | tests use `freshTmp()` + `HOME=dir, USERPROFILE=dir` env override on every spawn (mirrors lines 472, 486, 577 in existing tests); no test path constructs without the dir prefix |
</threat_model>

<verification>
- `npm run build` succeeds (tests must compile)
- `node --test build/cli.test.js` exits non-zero (RED is required for Wave 0)
- All NEW SKILL-* tests fail with assertion errors, not syntax errors
- All previously-passing SELFUP tests except the two deliberately retargeted assertions still pass
- The two retargeted SELFUP tests now FAIL with the new wording (this is intentional — Plan 02 makes them green)
</verification>

<success_criteria>
- 7 new test cases added: SKILL-01 (init writes legate.md), SKILL-04 (init writes legate-update.md), SKILL-05 (init overwrites idempotent), SKILL-03 ×3 (canonical loop + tool table; workers from registry; workers placeholder), SKILL-05 uninstall removes both
- 2 existing SELFUP assertions retargeted: warning-message wording, bogus-usage wording
- Suite is RED — every new SKILL test fails because Plan 02 has not yet implemented the production code
- File count unchanged: still 1 modified file (src/cli.test.ts)
</success_criteria>

<output>
After completion, create `.planning/phases/06-skill-card/06-01-SUMMARY.md` documenting:
- 7 new tests added (exact test names)
- 2 existing tests retargeted (exact lines edited)
- Test command and observed RED output (paste 5-10 lines of the failing test list)
- Confirmation that Plan 02 can now begin and will turn these GREEN
</output>
</content>
</invoke>